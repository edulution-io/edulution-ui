import FloatingButtonConfig from '@libs/common/types/floatingButtonConfig';

interface FloatingButtonsBarConfig {
  buttons: FloatingButtonConfig[];
  keyPrefix: string;
}

export default FloatingButtonsBarConfig;
