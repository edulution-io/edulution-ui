enum FileActionType {
  RENAME = 'name',
  CREATE_FOLDER = 'createFolder',
  CREATE_FILE = 'createFile',
  DELETE = '',
  UPLOAD_FILE = 'uploadFile',
  MOVE = 'locations',
}

export default FileActionType;
