---
phase: 03-cli-scaffolding
plan: 03
type: execute
wave: 2
depends_on: ['03-01']
files_modified:
  - edulution-ai-framework/cli/templates/styled-page/base/package.json
  - edulution-ai-framework/cli/templates/styled-page/base/index.html
  - edulution-ai-framework/cli/templates/styled-page/base/src/styles.css
  - edulution-ai-framework/cli/templates/styled-page/base/tailwind.config.js
  - edulution-ai-framework/cli/templates/styled-page/base/postcss.config.js
  - edulution-ai-framework/cli/templates/styled-page/base/_gitignore
  - edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/theme.css
  - edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/styles.css
  - edulution-ai-framework/cli/templates/styled-page/features/api-client/package.json
  - edulution-ai-framework/cli/templates/styled-page/features/api-client/src/api.js
  - edulution-ai-framework/cli/templates/styled-page/features/api-client/_env
  - edulution-ai-framework/cli/templates/styled-page/features/auth/package.json
  - edulution-ai-framework/cli/templates/styled-page/features/auth/src/auth.js
autonomous: true
requirements:
  - CLI-03

must_haves:
  truths:
    - 'Base styled-page template produces a buildable HTML+Tailwind project with edulution design tokens'
    - 'Tailwind feature overlay adds edulution theme CSS custom properties for light/dark mode'
    - 'API client feature overlay provides a vanilla JS fetch wrapper without React dependencies'
    - 'Auth feature overlay provides OIDC redirect flow using oidc-client-ts directly (no React wrapper)'
    - 'Styled pages use JavaScript (not TypeScript) for their feature overlays since they may not have a TS build step'
    - 'Base template includes Tailwind structurally (CSS build) since styled pages are Tailwind-centric'
  artifacts:
    - path: 'edulution-ai-framework/cli/templates/styled-page/base/package.json'
      provides: 'Tailwind CSS build dependencies and scripts'
      contains: 'tailwindcss'
    - path: 'edulution-ai-framework/cli/templates/styled-page/base/index.html'
      provides: 'HTML5 boilerplate with edulution branding'
      contains: '{{PROJECT_NAME}}'
    - path: 'edulution-ai-framework/cli/templates/styled-page/base/tailwind.config.js'
      provides: 'Edulution Tailwind preset for styled pages'
      contains: 'var(--primary)'
    - path: 'edulution-ai-framework/cli/templates/styled-page/base/src/styles.css'
      provides: 'Input CSS with @tailwind directives'
      contains: '@tailwind'
    - path: 'edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/theme.css'
      provides: 'CSS custom properties for light/dark edulution themes'
      contains: '--ci-light-green'
    - path: 'edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/styles.css'
      provides: 'Replacement styles.css with @import for theme.css'
      contains: '@import'
    - path: 'edulution-ai-framework/cli/templates/styled-page/features/api-client/src/api.js'
      provides: 'Vanilla JS fetch wrapper for edulution API'
      contains: 'credentials'
    - path: 'edulution-ai-framework/cli/templates/styled-page/features/api-client/_env'
      provides: 'Environment variable template with {{EDU_API_URL}} placeholder'
      contains: '{{EDU_API_URL}}'
    - path: 'edulution-ai-framework/cli/templates/styled-page/features/auth/src/auth.js'
      provides: 'OIDC redirect flow using oidc-client-ts without React'
      contains: 'UserManager'
  key_links:
    - from: 'edulution-ai-framework/cli/templates/styled-page/base/src/styles.css'
      to: 'edulution-ai-framework/cli/templates/styled-page/base/tailwind.config.js'
      via: '@tailwind directives processed by Tailwind CLI using the config'
      pattern: '@tailwind'
    - from: 'edulution-ai-framework/cli/templates/styled-page/features/auth/src/auth.js'
      to: 'edulution-ai-framework/cli/templates/styled-page/features/api-client/src/api.js'
      via: 'Auth provides tokens that API client sends with requests'
      pattern: 'credentials'
---

<objective>
Create all template files for the Styled HTML Page project type: a base HTML+Tailwind template and three feature overlay directories (Tailwind theme, API client, Auth).

Purpose: Styled pages are simpler than React apps -- they are standalone HTML/CSS pages with edulution branding. Tailwind is structural (always present in the base), but the theme CSS custom properties for light/dark mode are in the Tailwind feature overlay. API client and auth use vanilla JavaScript (no React). Tests feature is not available for styled pages (per user decision).

Output: Complete `templates/styled-page/` directory with base/ and features/ subdirectories.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cli-scaffolding/03-CONTEXT.md
@.planning/phases/03-cli-scaffolding/03-RESEARCH.md
@.planning/phases/03-cli-scaffolding/03-01-SUMMARY.md

Source patterns to reference:

- edulution-ai-framework/styled-page/AGENTS.md (styled page conventions, Tailwind config pattern)
- edulution-ai-framework/AGENTS.md (base conventions, design tokens)
- apps/frontend/src/index.scss (CSS custom properties source of truth)

<interfaces>
<!-- Types from Plan 01 -->

From cli/src/types.ts:

- PROJECT_TYPES.STYLED_PAGE = 'styled-page' (template directory name)
- Feature values: 'tailwind', 'api-client', 'auth' (no 'tests' for styled pages)

From cli/src/constants.ts:

- RENAME_MAP: \_gitignore -> .gitignore, \_env -> .env
- PLACEHOLDER_DEFAULTS: {{PROJECT_NAME}}, {{EDU_API_URL}}, {{AUTH_CLIENT_ID}}

Template structure convention:

- templates/styled-page/base/ -- always copied (includes Tailwind structurally)
- templates/styled-page/features/{feature}/ -- copied when feature selected
- package.json in each directory -- deep-merged
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create base styled-page template with structural Tailwind</name>
  <files>
    edulution-ai-framework/cli/templates/styled-page/base/package.json,
    edulution-ai-framework/cli/templates/styled-page/base/index.html,
    edulution-ai-framework/cli/templates/styled-page/base/src/styles.css,
    edulution-ai-framework/cli/templates/styled-page/base/tailwind.config.js,
    edulution-ai-framework/cli/templates/styled-page/base/postcss.config.js,
    edulution-ai-framework/cli/templates/styled-page/base/_gitignore
  </files>
  <action>
Create the base styled-page template at `templates/styled-page/base/`. Unlike custom-app, Tailwind is structural for styled pages (it IS the CSS approach), so the base includes Tailwind CSS build tooling.

**package.json:**

```json
{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --watch",
    "build:css": "npx tailwindcss -i ./src/styles.css -o ./dist/styles.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
```

**index.html** -- Full HTML5 boilerplate with edulution branding elements:

```html
<!doctype html>
<html
  lang="en"
  class="light"
>
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap"
      rel="stylesheet"
    />
    <link
      rel="stylesheet"
      href="./dist/styles.css"
    />
    <title>{{PROJECT_NAME}}</title>
  </head>
  <body class="min-h-screen bg-background font-sans text-foreground">
    <main class="mx-auto max-w-4xl p-8">
      <h1 class="mb-4 text-3xl font-bold text-primary">{{PROJECT_NAME}}</h1>
      <p class="text-muted-foreground">Scaffolded with edulution-create</p>
    </main>
  </body>
</html>
```

Note: The HTML uses Tailwind classes that reference the edulution color tokens (bg-background, text-foreground, text-primary, text-muted-foreground). These work because the tailwind.config.js maps these names to CSS custom properties, and the theme CSS (from the Tailwind feature overlay or inline in styles.css) provides the property values.

**src/styles.css** -- Input CSS with Tailwind directives and minimal base variables:

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --radius: 0.25rem;
    --ci-light-green: #8fc046;
    --ci-dark-blue: #0081c6;
    --background: #ffffff;
    --foreground: #000000;
    --primary: #0081c6;
    --primary-foreground: #ffffff;
    --muted-foreground: #505050;
    --border: #333333;
  }

  html {
    font-family: 'Lato', sans-serif;
  }
}
```

This provides minimal default values so the page works even without the Tailwind theme overlay. The theme overlay adds the full light/dark mode support.

**tailwind.config.js** (JavaScript, not TypeScript, since styled pages do not have a TS build step):

```javascript
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./**/*.html', './src/**/*.{js,css}'],
  darkMode: ['class'],
  theme: {
    extend: {
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        primary: { DEFAULT: 'var(--primary)', foreground: 'var(--primary-foreground)' },
        secondary: { DEFAULT: 'var(--secondary)', foreground: 'var(--secondary-foreground)' },
        destructive: { DEFAULT: 'var(--destructive)', foreground: 'var(--destructive-foreground)' },
        muted: { DEFAULT: 'var(--muted)', foreground: 'var(--muted-foreground)' },
        accent: { DEFAULT: 'var(--accent)', foreground: 'var(--accent-foreground)' },
        popover: { DEFAULT: 'var(--popover)', foreground: 'var(--popover-foreground)' },
        card: { DEFAULT: 'var(--card)', foreground: 'var(--card-foreground)' },
        border: 'var(--border)',
        input: 'var(--input)',
        ring: 'var(--ring)',
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      fontFamily: {
        sans: ['Lato', 'sans-serif'],
      },
    },
  },
  plugins: [],
};
```

**postcss.config.js:**

```javascript
module.exports = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**\_gitignore:**

```
node_modules
dist
*.local
.env
```

  </action>
  <verify>
    <automated>test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/styled-page/base/package.json && test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/styled-page/base/index.html && test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/styled-page/base/tailwind.config.js && grep -q "tailwindcss" /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/styled-page/base/package.json && echo "Styled page base template OK"</automated>
  </verify>
  <done>Base styled-page template exists with package.json (Tailwind dev deps + build:css script), index.html (full HTML5 with edulution branding, Lato font, Tailwind utility classes), styles.css (Tailwind directives + minimal default CSS variables), tailwind.config.js (edulution color mapping), postcss.config.js, and _gitignore. Template is buildable after npm install && npm run build:css.</done>
</task>

<task type="auto">
  <name>Task 2: Create Tailwind theme, API client, and Auth feature overlays for styled pages</name>
  <files>
    edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/theme.css,
    edulution-ai-framework/cli/templates/styled-page/features/tailwind/src/styles.css,
    edulution-ai-framework/cli/templates/styled-page/features/api-client/package.json,
    edulution-ai-framework/cli/templates/styled-page/features/api-client/src/api.js,
    edulution-ai-framework/cli/templates/styled-page/features/api-client/_env,
    edulution-ai-framework/cli/templates/styled-page/features/auth/package.json,
    edulution-ai-framework/cli/templates/styled-page/features/auth/src/auth.js
  </files>
  <action>
**Tailwind theme overlay** at `templates/styled-page/features/tailwind/`:

No package.json needed (Tailwind is already in the base). This overlay adds the full light/dark theme CSS custom properties.

**src/theme.css** -- Full edulution theme. This file is imported alongside styles.css (or the scaffold engine can prepend its content to styles.css). Use a separate file that the index.html links to:

```css
/* edulution theme - light and dark mode CSS custom properties */
/* Import this file in your HTML: <link rel="stylesheet" href="./src/theme.css" /> */

.light {
  --background: #000000;
  --foreground: #ffffff;
  --muted-foreground: #505050;
  --muted-background: #bbbbbb;
  --primary: #0081c6;
  --primary-foreground: #ffffff;
  --secondary: #2e2a24;
  --secondary-foreground: #c8c8c3;
  --muted: #b1b1ac;
  --accent: #c9ccd2;
  --accent-foreground: #1d170f;
  --input: #1d170f;
  --card: #00000000;
  --card-foreground: #000000;
  --popover: #000000;
  --popover-foreground: #f0e8d5;
  --border: #333333;
  --destructive: #ff0000;
  --destructive-foreground: #130800;
  --ring: #735b49;
  --overlay: #ffffff;
}

.dark {
  --background: #ffffff;
  --foreground: #000000;
  --muted-foreground: #bbbbbb;
  --muted-background: #1a1a1a;
  --primary: #0081c6;
  --primary-foreground: #ffffff;
  --secondary: #d1d5db;
  --secondary-foreground: #2d2d30;
  --muted: #2d3748;
  --accent: #2d2d30;
  --accent-foreground: #d1d5db;
  --input: #e2e8f0;
  --card: #00000000;
  --card-foreground: #ffffff;
  --popover: #ffffff;
  --popover-foreground: #0f172a;
  --border: #cbd4e4;
  --destructive: #ff0000;
  --destructive-foreground: #ecf7ff;
  --ring: #8ca4b6;
  --overlay: #1e2938;
}
```

**src/styles.css** -- Replacement for the base styles.css that adds @import for theme.css and removes inline default values (they come from theme.css now):

```css
@import './theme.css';

@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --radius: 0.25rem;
    --ci-light-green: #8fc046;
    --ci-dark-blue: #0081c6;
  }

  html {
    font-family: 'Lato', sans-serif;
  }
}
```

This overlay file overwrites the base `src/styles.css` to add the @import for theme.css and remove the inline default color values (which are now provided by theme.css via the light/dark classes).

**API client overlay** at `templates/styled-page/features/api-client/`:

No React -- use vanilla JS with the fetch API. Per Claude's discretion from CONTEXT.md on "how to handle styled page feature combinations (API client without React)".

**package.json** (fragment -- empty deps, but adds a comment via description):

```json
{
  "description": "Includes edulution API client"
}
```

No npm dependencies needed -- uses native `fetch`. If the user also selects auth, they get `oidc-client-ts` from the auth overlay.

**src/api.js:**

```javascript
const API_BASE_URL = '{{EDU_API_URL}}';

const apiFetch = async (path, options = {}) => {
  const url = `${API_BASE_URL}${path}`;
  const response = await fetch(url, {
    credentials: 'include',
    headers: {
      'Content-Type': 'application/json',
      ...options.headers,
    },
    ...options,
  });
  if (!response.ok) {
    throw new Error(`API error: ${response.status} ${response.statusText}`);
  }
  return response.json();
};

const apiGet = (path) => apiFetch(path);

const apiPost = (path, body) =>
  apiFetch(path, {
    method: 'POST',
    body: JSON.stringify(body),
  });

export { apiFetch, apiGet, apiPost };
```

**\_env** (renamed to `.env` during scaffold):

```
EDU_API_URL={{EDU_API_URL}}
```

**Auth overlay** at `templates/styled-page/features/auth/`:

Uses `oidc-client-ts` directly without React wrapper. Implements redirect-based OIDC flow per Research open question recommendation.

**package.json** (fragment):

```json
{
  "dependencies": {
    "oidc-client-ts": "^3.1.0"
  }
}
```

**src/auth.js** -- OIDC redirect flow using vanilla JS:

```javascript
import { UserManager, WebStorageStateStore } from 'oidc-client-ts';

const EDU_API_URL = '{{EDU_API_URL}}';

const userManager = new UserManager({
  authority: `${EDU_API_URL}/auth`,
  client_id: '{{AUTH_CLIENT_ID}}',
  redirect_uri: window.location.origin,
  loadUserInfo: true,
  automaticSilentRenew: true,
  userStore: new WebStorageStateStore({ store: localStorage }),
});

const login = () => userManager.signinRedirect();

const logout = () => userManager.signoutRedirect();

const getUser = () => userManager.getUser();

const handleCallback = async () => {
  if (window.location.search.includes('code=')) {
    const user = await userManager.signinRedirectCallback();
    window.history.replaceState({}, document.title, window.location.pathname);
    return user;
  }
  return null;
};

const getAccessToken = async () => {
  const user = await userManager.getUser();
  return user?.access_token ?? null;
};

export { login, logout, getUser, handleCallback, getAccessToken };
```

After creating all files, verify the complete styled-page template structure.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/styled-page && test -d base && test -d features/tailwind && test -d features/api-client && test -d features/auth && grep -q "ci-light-green" features/tailwind/src/theme.css && test -f features/tailwind/src/styles.css && test -f features/api-client/src/api.js && test -f features/api-client/\_env && grep -q "UserManager" features/auth/src/auth.js && echo "All styled-page template directories and key files present"</automated>
</verify>
<done>Tailwind theme overlay exists with theme.css (full light/dark CSS custom properties) and replacement styles.css (with @import for theme). API client overlay exists with vanilla JS fetch wrapper (apiFetch, apiGet, apiPost) using credentials: 'include' and \_env template file. Auth overlay exists with oidc-client-ts UserManager setup (login, logout, getUser, handleCallback, getAccessToken). Complete styled-page template set verified: base/ + 3 feature overlays (tailwind, api-client, auth). No tests feature directory (tests excluded for styled pages per user decision).</done>
</task>

</tasks>

<verification>
- All template directories exist: `templates/styled-page/{base,features/tailwind,features/api-client,features/auth}`
- No `features/tests` directory exists for styled pages (tests excluded per user decision)
- Base includes Tailwind structurally (tailwind.config.js, postcss.config.js, styles.css with @tailwind)
- Theme overlay has complete light/dark CSS custom property definitions matching edulution-ui source values
- Theme overlay includes replacement styles.css with @import for theme.css
- API client uses native fetch (no axios, no React) and includes _env template
- Auth uses oidc-client-ts directly (no react-oidc-context)
- All JS files use plain JavaScript (not TypeScript) since styled pages may not have a TS build step
- All {{PLACEHOLDER}} markers match the constants from Plan 01 ({{PROJECT_NAME}}, {{EDU_API_URL}}, {{AUTH_CLIENT_ID}})
</verification>

<success_criteria>

- Base styled-page template is buildable (npm install && npm run build:css produces CSS output)
- index.html has edulution branding with Lato font, Tailwind utility classes, and {{PROJECT_NAME}} placeholder
- Tailwind theme overlay provides full light/dark mode switching via CSS classes
- Tailwind theme overlay includes replacement styles.css with @import for theme.css
- API client overlay uses vanilla JS fetch with credentials: 'include' and \_env template
- Auth overlay uses oidc-client-ts UserManager for redirect-based OIDC flow without React
- Feature overlays use plain JavaScript (.js) not TypeScript
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cli-scaffolding/03-03-SUMMARY.md`
</output>
