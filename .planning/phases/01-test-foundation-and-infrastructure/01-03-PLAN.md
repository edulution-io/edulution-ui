---
phase: 01-test-foundation-and-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - .github/workflows/build-and-test.yml
  - apps/frontend/src/store/utils/cleanAllStores.ts
  - apps/frontend/src/store/utils/cleanAllStores.spec.ts
autonomous: true
requirements:
  - FOUND-08
  - FOUND-09
  - CICD-01
  - CICD-02

must_haves:
  truths:
    - 'The CI pipeline has a dedicated test-frontend job that runs in parallel with test-api'
    - 'Both test-frontend and test-api are separate jobs in the CI pipeline'
    - 'All Zustand stores are registered in cleanAllStores.ts'
    - 'An automated validation test fails if a new store is added without registration'
  artifacts:
    - .github/workflows/build-and-test.yml
    - apps/frontend/src/store/utils/cleanAllStores.spec.ts
  key_links:
    - 'test-frontend job runs npm run test:frontend'
    - 'test-api job runs npm run test (existing API tests)'
    - 'cleanAllStores.spec.ts scans filesystem for store files and asserts all are registered'
---

<objective>
Split the CI test job into parallel test-frontend and test-api jobs, register all 21 missing Zustand stores in cleanAllStores.ts, and create an automated validation test that enforces store registration.

Purpose: CI blocks PRs on frontend test failures from day one, and the cleanAllStores validation prevents stores from being added without proper cleanup registration.
Output: Updated CI workflow with parallel test jobs, complete store registration, and a validation test.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-test-foundation-and-infrastructure/01-RESEARCH.md
@.github/workflows/build-and-test.yml
@apps/frontend/src/store/utils/cleanAllStores.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split CI test job into parallel test-frontend and test-api jobs</name>
  <files>
    .github/workflows/build-and-test.yml
  </files>
  <action>
Read the current `.github/workflows/build-and-test.yml`. The existing `test` job runs:
```
npm run check-circular-deps
npm run check-translations
npm run check-error-message-translations
npx pretty-quick --check
npm run lint
npm run test
```

Refactor into three jobs:

1. **`checks`** job (rename from `test`): Runs lint and static checks only:

   ```yaml
   checks:
     runs-on: ubuntu-24.04
     needs: prepare
     timeout-minutes: 15
     steps:
       - Checkout, Node setup, cache restore (same as current)
       - name: Run Checks
         run: |
           npm run check-circular-deps
           npm run check-translations
           npm run check-error-message-translations
           npx pretty-quick --check
           npm run lint
   ```

2. **`test-api`** job: Runs API tests only:

   ```yaml
   test-api:
     runs-on: ubuntu-24.04
     needs: prepare
     timeout-minutes: 15
     steps:
       - Checkout, Node setup, cache restore (same as current)
       - name: Run API Tests
         run: npm run test
   ```

3. **`test-frontend`** job: Runs frontend tests:
   ```yaml
   test-frontend:
     runs-on: ubuntu-24.04
     needs: prepare
     timeout-minutes: 15
     steps:
       - Checkout, Node setup, cache restore (same as current)
       - name: Run Frontend Tests
         run: npm run test:frontend
   ```

All three jobs depend on `prepare` and run in parallel. Ensure `build-frontend` and `build-api` still only depend on `prepare` (not on the test jobs -- builds and tests run in parallel). Keep the same `if: github.event.pull_request.draft == false` condition on the prepare job.

Match the exact style of existing jobs: same actions versions (checkout@v5.0.0, setup-node@v6.0.0, cache@v4.3.0), same cache key pattern, same node-version (22.x).
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && python3 -c "
import yaml
with open('.github/workflows/build-and-test.yml') as f:
wf = yaml.safe_load(f)
jobs = list(wf['jobs'].keys())
assert 'test-frontend' in jobs, 'test-frontend job missing'
assert 'test-api' in jobs, 'test-api job missing'
assert 'checks' in jobs, 'checks job missing'
print('PASS: All three jobs present:', jobs)
"</automated>
</verify>
<done>CI pipeline has three parallel jobs: checks (lint+static), test-api (Jest), test-frontend (Vitest). All run in parallel after prepare. Build jobs are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Register missing Zustand stores and create validation test</name>
  <files>
    apps/frontend/src/store/utils/cleanAllStores.ts
    apps/frontend/src/store/utils/cleanAllStores.spec.ts
  </files>
  <action>
**Part A: Register missing stores**

First, scan for ALL Zustand store files:

```bash
find apps/frontend/src -name "use*Store.ts" -o -name "use*Store.tsx" | sort
```

Then read `cleanAllStores.ts` to find which stores are already registered. The research identified 21 missing stores. For each missing store:

1. Check if the store has a `reset()` method or equivalent state reset function by reading the store file
2. If the store has `reset()`: Add an import and call `storeInstance.getState().reset()` in the `cleanAllStores` function
3. If the store does NOT have a `reset()` method: Add a `reset` function to the store (follow existing pattern -- `reset: () => set(initialState)` or similar). Then add the import and reset call to cleanAllStores.ts.

Known missing stores (verify against actual filesystem):

- useSharePublicConferenceStore
- useHandleUploadFileStore
- useReplaceFilesDialogStore
- useFileContentPreviewStore
- useFileEditorContentStore
- useDownloadAcknowledgedStore
- useFileSharingMoveDialogStore
- useDockerApplicationStore
- useWebdavServerConfigTableStore
- useWireguardConfigTableStore
- useWebhookClientsStore
- useExportSurveyToPdfStore
- useUserSettingsPageStore
- useUserWireguardStore
- useWhiteboardEditorStore
- usePlatformStore
- useSentryStore
- useSubMenuStore
- useTableViewSettingsStore
- useThemeStore
- useUserPreferencesStore

Maintain alphabetical ordering in both the import block and the function body (matching the existing pattern in cleanAllStores.ts).

**Part B: Create validation test**

Create `apps/frontend/src/store/utils/cleanAllStores.spec.ts`:

The test should:

1. Use `fs` and `path` to recursively scan `apps/frontend/src/` for files matching the pattern `use*Store.ts` and `use*Store.tsx`
2. Read `cleanAllStores.ts` source and extract all imported store names
3. Compare the filesystem store list against the imported store list
4. Account for special cases:
   - `UserStore` (imported as `UserStore` from `useUserStore.ts` -- different naming)
   - `useSseStore` (imported with relative path `../useSseStore`)
   - Stores may have multiple reset method calls (e.g., UserStore has resetQrCodeSlice, resetTotpSlice, etc.)
5. Fail with a descriptive message listing unregistered stores

The test description: `'should register all Zustand stores in cleanAllStores'`

Use `describe('cleanAllStores validation', () => { ... })` block.

The test should use Vitest APIs (this is a frontend test): `import { describe, it, expect } from 'vitest'`.

Include the project license header.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx vitest run apps/frontend/src/store/utils/cleanAllStores.spec.ts --reporter=verbose 2>&1 | tail -20</automated>
</verify>
<done>All 74 Zustand stores registered in cleanAllStores.ts. Validation test passes confirming every store file has a corresponding import in cleanAllStores.ts. Adding a new store without registration will fail the test.</done>
</task>

</tasks>

<verification>
1. CI workflow has `test-frontend`, `test-api`, and `checks` as separate parallel jobs
2. `npm run test:frontend` passes including the cleanAllStores validation test
3. `npm run test` (API) still passes
4. cleanAllStores.ts imports and resets all 74 Zustand stores
5. cleanAllStores.spec.ts would fail if a new store is added without registration (can verify by temporarily removing one import)
</verification>

<success_criteria>

- CI pipeline runs frontend and API tests in parallel as separate jobs
- All Zustand stores are registered in cleanAllStores.ts
- Automated validation test enforces store registration
- No existing tests broken
  </success_criteria>

<output>
After completion, create `.planning/phases/01-test-foundation-and-infrastructure/01-03-SUMMARY.md`
</output>
