---
phase: 02-api-unit-test-expansion
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/mails/mails.service.spec.ts
  - apps/api/src/mails/mails.controller.spec.ts
  - apps/api/src/notifications/notifications.service.spec.ts
  - apps/api/src/notifications/notifications.controller.spec.ts
  - apps/api/src/conferences/conferences.service.spec.ts
  - apps/api/src/conferences/conferences.controller.spec.ts
autonomous: true
requirements:
  - APIT-07
  - APIT-09
  - APIT-10
  - APIT-12

must_haves:
  truths:
    - 'mails.service.spec.ts tests getMails happy path and IMAP error handling, getSyncJobs with Mailcow API, create/delete sync jobs, and external mail provider config CRUD'
    - 'mails.service.spec.ts mocks at the ImapFlow client level and Mailcow Axios client level, not at the protocol level'
    - 'notifications.service.spec.ts tests notifyUsernames with rollback on error, upsertNotificationForSource, cascadeDeleteBySourceId, createUserNotifications batching, markAsRead/markAllAsRead, deleteUserNotification with orphan cleanup'
    - 'notifications.service.spec.ts mocks Expo-specific error response types (DeviceNotRegistered, MessageTooBig, InvalidCredentials)'
    - 'conferences.service.spec.ts is deepened with error paths for BBB API failures, edge cases for join URL generation, and findAll with no conferences'
    - 'Controller specs for mails, notifications, and conferences verify service delegation with correct arguments'
  artifacts:
    - apps/api/src/mails/mails.service.spec.ts
    - apps/api/src/mails/mails.controller.spec.ts
    - apps/api/src/notifications/notifications.service.spec.ts
    - apps/api/src/notifications/notifications.controller.spec.ts
    - apps/api/src/conferences/conferences.service.spec.ts
    - apps/api/src/conferences/conferences.controller.spec.ts
  key_links:
    - 'mails.service.spec.ts mocks ImapFlow (connect, getMailboxLock, search, fetch, logout, close) and Mailcow Axios client'
    - 'notifications.service.spec.ts mocks PushNotificationQueue.enqueue, not Expo SDK directly'
    - 'conferences.service.spec.ts imports createTestingModule replacing raw Test.createTestingModule'
---

<objective>
Create comprehensive unit tests for the three remaining core platform services: mail, notifications, and conferences. Each service gets both a service spec and a controller spec in the same plan per user decision.

Purpose: Mail, notifications, and conferences are core platform services with complex external integrations (IMAP, Expo push, BigBlueButton). Testing these services catches integration-boundary failures before they reach users.
Output: 3 service spec files and 3 controller spec files with behavioral assertions covering happy paths, error paths, and external service failure handling.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-unit-test-expansion/02-RESEARCH.md
@apps/api/src/mails/mails.service.ts
@apps/api/src/mails/mails.controller.ts
@apps/api/src/notifications/notifications.service.ts
@apps/api/src/notifications/notifications.controller.ts
@apps/api/src/conferences/conferences.service.ts
@apps/api/src/conferences/conferences.service.spec.ts
@apps/api/src/conferences/conferences.controller.ts
@apps/api/src/conferences/conferences.controller.spec.ts
@libs/src/test-utils/api-mocks/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mail service and controller specs</name>
  <files>
    apps/api/src/mails/mails.service.spec.ts
    apps/api/src/mails/mails.controller.spec.ts
  </files>
  <action>
**mails.service.spec.ts -- New file:**

1. Read `mails.service.ts` thoroughly to understand all public methods, constructor dependencies, and external integrations.
2. Note the **PITFALL**: MailsService reads `process.env` at module level (MAILCOW_API_URL, etc.). Set required env vars in `beforeAll` or test behavior when env vars are missing.
3. Note the **PITFALL**: If MailsService has `onModuleInit`, mock or spy on it to prevent real network calls during test compilation.

Mock at the service boundary per user decision:

- **ImapFlow client**: Mock the class or its instances. Mock methods: `connect()`, `getMailboxLock()`, `search()`, `fetch()` (returns async iterable), `logout()`, `close()`. The service likely creates ImapFlow instances -- mock the constructor.
- **Mailcow Axios client**: Mock the Axios instance used for Mailcow API calls (getSyncJobs, createSyncJob, deleteSyncJob, external mail provider config CRUD).
- **Other dependencies**: Check constructor for Docker, MailProvider model, etc. Mock all injected dependencies.

Use `createTestingModule` from Phase 1 infrastructure. Use `createMongooseModelMock` if the service uses Mongoose models. Use `createJwtUser` for user fixtures.

Test cases (every public method, happy-path + error-path):

- **getMails**: Happy path (IMAP connects, fetches messages, returns formatted mail list). Error path (IMAP connection fails, returns empty array or throws).
- **getSyncJobs**: Happy path (Mailcow API returns jobs). Error path (API call fails).
- **createSyncJob / deleteSyncJob**: Happy path (API succeeds). Error path (API returns error).
- **getExternalMailProviderConfig / postExternalMailProviderConfig / deleteExternalMailProviderConfig**: CRUD operations against Mailcow API.
- **getMails with empty mailbox**: Returns empty array.
- **getMails when IMAP config is missing**: Returns empty array (graceful degradation per pitfall guidance).

**mails.controller.spec.ts -- New file:**

Read `mails.controller.ts` to understand all endpoints.

Create controller spec:

- Mock MailsService entirely (controller tests verify delegation only)
- Test each endpoint:
  - Verify correct service method called with correct arguments
  - Verify response matches service return value
  - Verify user/auth context is passed through correctly

Include project license header on all files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "mails" --no-coverage -x 2>&1 | tail -20</automated>
</verify>
<done>mails.service.spec.ts passes with tests covering getMails (happy + IMAP error), getSyncJobs, create/delete sync jobs, external mail provider CRUD, and graceful degradation when IMAP config is missing. mails.controller.spec.ts verifies delegation for all endpoints. ImapFlow and Mailcow Axios are mocked at the client level.</done>
</task>

<task type="auto">
  <name>Task 2: Create notifications service and controller specs, deepen conferences specs</name>
  <files>
    apps/api/src/notifications/notifications.service.spec.ts
    apps/api/src/notifications/notifications.controller.spec.ts
    apps/api/src/conferences/conferences.service.spec.ts
    apps/api/src/conferences/conferences.controller.spec.ts
  </files>
  <action>
**notifications.service.spec.ts -- New file:**

1. Read `notifications.service.ts` thoroughly (720 lines, complex service).
2. Note the **PITFALL**: If it has `onModuleInit` or scheduled tasks, mock those to prevent side effects.

Mock dependencies:

- **PushNotificationQueue**: Mock `enqueue` method (per user decision: mock the queue, not Expo directly)
- **Notification Mongoose model**: Use `createMongooseModelMock`
- **UserNotification Mongoose model**: Use `createMongooseModelMock`
- **UsersService**: Mock methods called by NotificationsService
- **SseService**: Mock methods called by NotificationsService

Use `createTestingModule`, `createMongooseModelMock`, `createJwtUser`.

Test cases (every public method, happy-path + error-path per user decision):

- **notifyUsernames**: Happy path (notifications created for all users, push enqueued). Error path (test rollback behavior when push queue fails).
- **upsertNotificationForSource**: New notification (creates new). Existing notification (updates existing). Verify source ID linkage.
- **cascadeDeleteBySourceId**: Deletes all notifications for a given source. No matching notifications (no-op).
- **createUserNotifications**: Test batching logic for multiple users. Empty user list (no-op).
- **markAsRead / markAllAsRead**: Mark single notification read. Mark all for user. Already-read notification (idempotent).
- **deleteUserNotification**: Delete notification. Verify orphan cleanup (if the notification has no more user-notifications, delete the parent notification).
- **shouldSendPush**: Test debounce logic if applicable.
- **Expo-specific error types** per user decision: Mock PushNotificationQueue to simulate DeviceNotRegistered, MessageTooBig, InvalidCredentials errors and verify the service handles each correctly (e.g., removes invalid push tokens, logs appropriate errors).

**notifications.controller.spec.ts -- New file:**

Read `notifications.controller.ts` for all endpoints.

- Mock NotificationsService
- Test delegation for each endpoint with correct args
- Verify user context passed through

**conferences.service.spec.ts -- Deepen existing:**

1. Read current spec (13 existing tests).
2. Read `conferences.service.ts` for methods not yet covered.

Refactor setup to use Phase 1 infrastructure (`createTestingModule`, `createMongooseModelMock`).

Add new tests:

- **BBB API failure paths**: Non-SUCCESS returncode from BBB API calls (create, join, start, stop). Verify error is handled correctly.
- **Join URL generation edge cases**: Missing meeting ID, meeting not started, user not authorized
- **findAll with no conferences**: Returns empty array
- **Error paths for existing methods**: Test what happens when Mongoose operations fail

**conferences.controller.spec.ts -- Deepen existing:**

Read current spec and add tests for any untested endpoints. Ensure delegation verification pattern is consistent.

Include project license header on all new files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "notifications|conferences" --no-coverage -x 2>&1 | tail -30</automated>
</verify>
<done>notifications.service.spec.ts passes with tests for notifyUsernames (happy + rollback), upsertNotificationForSource, cascadeDeleteBySourceId, createUserNotifications batching, markAsRead/markAllAsRead, deleteUserNotification with orphan cleanup, and Expo-specific error types. notifications.controller.spec.ts verifies all endpoints. conferences.service.spec.ts deepened with BBB API failure paths and edge cases. conferences.controller.spec.ts deepened.</done>
</task>

</tasks>

<verification>
1. `npx jest --config apps/api/jest.config.ts --testPathPattern "mails" --no-coverage` -- mail specs pass
2. `npx jest --config apps/api/jest.config.ts --testPathPattern "notifications" --no-coverage` -- notification specs pass
3. `npx jest --config apps/api/jest.config.ts --testPathPattern "conferences" --no-coverage` -- conference specs pass (including deepened tests)
4. No env vars leak from tests (process.env set in beforeAll, cleaned in afterAll)
5. `npm run test` -- full API test suite still passes
</verification>

<success_criteria>

- Mail service tests mock at ImapFlow and Mailcow Axios client level (not protocol level)
- Mail service tests cover getMails, getSyncJobs, sync job CRUD, external mail provider CRUD with happy and error paths
- Notification service tests cover all major public methods with happy and error paths
- Notification service tests mock Expo-specific error types (DeviceNotRegistered, MessageTooBig, InvalidCredentials)
- Conference service tests are deepened with BBB API failure paths and edge cases
- All three modules have both service and controller spec files
  </success_criteria>

<output>
After completion, create `.planning/phases/02-api-unit-test-expansion/02-04-SUMMARY.md`
</output>
