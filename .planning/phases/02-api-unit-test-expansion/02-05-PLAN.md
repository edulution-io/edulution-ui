---
phase: 02-api-unit-test-expansion
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/docker/docker.service.spec.ts
  - apps/api/src/docker/docker.controller.spec.ts
  - apps/api/src/ldap-keycloak-sync/ldap-keycloak-sync.service.spec.ts
  - apps/api/src/wireguard/wireguard.service.spec.ts
  - apps/api/src/wireguard/wireguard.controller.spec.ts
  - apps/api/src/webdav/webdav.service.spec.ts
autonomous: true
requirements:
  - APIT-11
  - APIT-12
  - APIT-13

must_haves:
  truths:
    - 'DockerService tests mock at dockerode library level (container.start(), container.stop(), listContainers, createContainer, pull) and cover container lifecycle operations'
    - 'LdapKeycloakSyncService tests use stateful mocks that track sync state across calls to verify the full sync pipeline (user created in LDAP -> synced to Keycloak -> groups mapped)'
    - 'WireguardService spec is deepened beyond "should be defined" with behavioral tests for VPN config CRUD and tunnel start/stop'
    - 'WebdavService tests cover file operations (create, read, move, delete), directory operations, and error paths'
  artifacts:
    - apps/api/src/docker/docker.service.spec.ts
    - apps/api/src/docker/docker.controller.spec.ts
    - apps/api/src/ldap-keycloak-sync/ldap-keycloak-sync.service.spec.ts
    - apps/api/src/wireguard/wireguard.service.spec.ts
    - apps/api/src/wireguard/wireguard.controller.spec.ts
    - apps/api/src/webdav/webdav.service.spec.ts
  key_links:
    - 'DockerService spec mocks dockerode Docker class and Container instances'
    - 'LdapKeycloakSyncService spec uses stateful mocks tracking sync state for the full pipeline test'
    - 'All service specs use createTestingModule and createMongooseModelMock from Phase 1 infrastructure'
    - 'Wireguard controller spec deepened with delegation verification for all endpoints'
---

<objective>
Create unit tests for the high-complexity infrastructure services: Docker (607 lines, dockerode integration), LDAP-Keycloak sync (865 lines, stateful sync pipeline), Wireguard (deepening), and Webdav (465 lines, file operations). These are the most complex infrastructure services requiring specialized mock strategies.

Purpose: Docker manages container lifecycle, LDAP-Keycloak sync manages identity synchronization (critical security surface), Wireguard manages VPN tunnels, and Webdav handles file storage operations. Failures in any of these break core system functionality.
Output: 4 service spec files (2 new, 2 deepened) and 2 controller spec files with behavioral assertions covering happy paths, error paths, and external service failure handling.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-unit-test-expansion/02-RESEARCH.md
@apps/api/src/docker/docker.service.ts
@apps/api/src/docker/docker.controller.ts
@apps/api/src/ldap-keycloak-sync/ldap-keycloak-sync.service.ts
@apps/api/src/wireguard/wireguard.service.ts
@apps/api/src/wireguard/wireguard.service.spec.ts
@apps/api/src/wireguard/wireguard.controller.ts
@apps/api/src/wireguard/wireguard.controller.spec.ts
@apps/api/src/webdav/webdav.service.ts
@libs/src/test-utils/api-mocks/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create DockerService and LdapKeycloakSyncService specs</name>
  <files>
    apps/api/src/docker/docker.service.spec.ts
    apps/api/src/docker/docker.controller.spec.ts
    apps/api/src/ldap-keycloak-sync/ldap-keycloak-sync.service.spec.ts
  </files>
  <action>
For each service, read the source file first to understand public methods, constructor dependencies, and external integrations. Use `createTestingModule` and `createMongooseModelMock` from Phase 1 infrastructure throughout.

**DockerService spec** (`apps/api/src/docker/docker.service.spec.ts`):

- Read `docker.service.ts` (607 lines)
- **PITFALL**: DockerService reads `process.env` and may have `onModuleInit`. Set env vars in beforeAll and mock/spy onModuleInit.
- Mock at dockerode library level per user decision:
  - Mock the `Docker` class from `dockerode`
  - Mock `Container` instances with methods: `start()`, `stop()`, `restart()`, `remove()`, `inspect()`
  - Mock `Docker` methods: `getContainer()`, `listContainers()`, `createContainer()`, `pull()`
- Test cases:
  - List containers: happy path (returns formatted list), error path (dockerode throws)
  - Start/stop/restart container: happy path, container not found error
  - Get container status: running, stopped, not found
  - Create container: happy path with valid config
  - Pull image: happy path, image not found

**DockerController spec** (`apps/api/src/docker/docker.controller.spec.ts`):

- Read `docker.controller.ts` (check if it exists first -- skip if no controller)
- Mock DockerService
- Verify delegation with correct arguments for each endpoint

**LdapKeycloakSyncService spec** (`apps/api/src/ldap-keycloak-sync/ldap-keycloak-sync.service.spec.ts`):

- Read `ldap-keycloak-sync.service.ts` (865 lines -- most complex infrastructure service)
- **PITFALL**: Has `onModuleInit` and likely scheduled sync tasks. Mock lifecycle hooks.
- **User decision**: Use stateful mocks that track sync state across calls to verify the full sync pipeline (user created in LDAP -> synced to Keycloak -> groups mapped)
- Mock dependencies:
  - LDAP client (or the service that wraps it)
  - Keycloak API client (or KeycloakRequestQueue)
  - GroupsService
  - Cache manager (from Phase 1 infrastructure)
  - LdapKeycloakSync Mongoose model: use `createMongooseModelMock`
- Create stateful mocks: mocks that maintain internal state dictionaries so you can verify:
  1. LDAP query returns users
  2. Keycloak createUser is called for each LDAP user
  3. Group mappings are applied correctly
  4. Subsequent sync calls detect already-synced users
- Test cases:
  - Full sync pipeline: LDAP users -> Keycloak users + groups
  - Incremental sync: new users added, existing users unchanged
  - User deletion: user removed from LDAP -> removed from Keycloak
  - Group change: user group changes in LDAP -> updated in Keycloak
  - Sync error recovery: failure mid-sync -> partial state is handled

Include project license header on all new files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "docker.service.spec|docker.controller.spec|ldap-keycloak-sync" --no-coverage -x 2>&1 | tail -30</automated>
</verify>
<done>DockerService spec passes with dockerode mocks covering container lifecycle. LdapKeycloakSyncService spec passes with stateful mocks verifying full sync pipeline (LDAP -> Keycloak -> groups). DockerController spec created if controller exists.</done>
</task>

<task type="auto">
  <name>Task 2: Deepen WireguardService and create WebdavService specs</name>
  <files>
    apps/api/src/wireguard/wireguard.service.spec.ts
    apps/api/src/wireguard/wireguard.controller.spec.ts
    apps/api/src/webdav/webdav.service.spec.ts
  </files>
  <action>
**Deepen WireguardService spec** (`apps/api/src/wireguard/wireguard.service.spec.ts`):
- Read current spec and `wireguard.service.ts`
- If spec only has "should be defined" or shallow tests, add comprehensive behavioral tests
- Refactor to use Phase 1 infrastructure if not already using it
- Mock Axios API client used for WireGuard operations
- Test cases:
  - VPN config CRUD: create config (happy path), get config, delete config, config not found
  - Tunnel start/stop: happy path, tunnel already running, tunnel not found
  - Error handling: API client throws, timeout

**Deepen WireguardController spec** (`apps/api/src/wireguard/wireguard.controller.spec.ts`):

- Read current spec and add tests for remaining untested controller endpoints
- Verify delegation pattern is consistent with correct arguments

**WebdavService spec** (`apps/api/src/webdav/webdav.service.spec.ts`):

- Read `webdav.service.ts` (465 lines)
- Mock: WebdavClientFactory (creates webdav client instances), UsersService, WebdavSharesService, `got` library if used
- Test cases:
  - File operations: create file (happy path), read file, move file, delete file
  - Directory operations: list directory contents, create directory
  - Error paths: file not found (404), permission denied (403), storage full
  - Edge cases: special characters in file names, deeply nested paths

Include project license header on all new files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "wireguard|webdav.service.spec" --no-coverage -x 2>&1 | tail -30</automated>
</verify>
<done>WireguardService spec deepened with VPN config CRUD and tunnel start/stop tests. WireguardController spec deepened with delegation verification. WebdavService spec created with file/directory operation tests and error path coverage.</done>
</task>

</tasks>

<verification>
1. `npx jest --config apps/api/jest.config.ts --testPathPattern "docker" --no-coverage` -- Docker specs pass
2. `npx jest --config apps/api/jest.config.ts --testPathPattern "ldap-keycloak-sync" --no-coverage` -- LDAP sync spec passes with stateful mocks
3. `npx jest --config apps/api/jest.config.ts --testPathPattern "wireguard" --no-coverage` -- Wireguard specs pass (deepened)
4. `npx jest --config apps/api/jest.config.ts --testPathPattern "webdav.service.spec" --no-coverage` -- Webdav spec passes
5. `npm run test` -- full API test suite still passes
</verification>

<success_criteria>

- DockerService mocks at dockerode library level with container lifecycle coverage
- LdapKeycloakSyncService uses stateful mocks for full sync pipeline verification
- WireguardService deepened beyond "should be defined" with behavioral tests
- WebdavService covers file/directory operations with error paths
- All specs use Phase 1 infrastructure (createTestingModule, createMongooseModelMock)
- Full API test suite passes with no regressions
  </success_criteria>

<output>
After completion, create `.planning/phases/02-api-unit-test-expansion/02-05-SUMMARY.md`
</output>
