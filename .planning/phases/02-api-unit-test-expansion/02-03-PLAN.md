---
phase: 02-api-unit-test-expansion
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/surveys/surveys.service.spec.ts
  - apps/api/src/surveys/surveys.controller.spec.ts
  - apps/api/src/surveys/survey-answers.service.spec.ts
  - apps/api/src/surveys/public-surveys.controller.spec.ts
  - apps/api/src/filesharing/filesharing.service.spec.ts
  - apps/api/src/filesharing/filesharing.controller.spec.ts
autonomous: true
requirements:
  - APIT-06
  - APIT-08
  - APIT-12
  - APIT-13

must_haves:
  truths:
    - 'surveys.service.spec.ts has all previously commented-out tests uncommented and passing with behavioral assertions for scoring, participation, results, and permissions'
    - 'surveys.service.spec.ts uses createTestingModule and createMongooseModelMock from Phase 1 infrastructure'
    - 'filesharing.service.spec.ts tests the REAL FilesharingService class (not a mock), with mocked dependencies'
    - 'filesharing.service.spec.ts covers upload, delete, share (public share CRUD), collect operations, and file access rights'
    - 'surveys.controller.spec.ts and filesharing.controller.spec.ts verify delegation to service methods with correct arguments and response shapes'
    - 'survey-answers.service.spec.ts and public-surveys.controller.spec.ts are deepened with behavioral assertions'
  artifacts:
    - apps/api/src/surveys/surveys.service.spec.ts
    - apps/api/src/surveys/surveys.controller.spec.ts
    - apps/api/src/filesharing/filesharing.service.spec.ts
    - apps/api/src/filesharing/filesharing.controller.spec.ts
  key_links:
    - 'surveys.service.spec.ts imports createTestingModule and createMongooseModelMock replacing manual mock setup'
    - 'filesharing.service.spec.ts provides real FilesharingService with mocked WebdavService, OnlyofficeService, FilesystemService, QueueService, UsersService, WebdavSharesService, PublicShare model'
    - 'Controller specs verify service methods are called with correct DTO arguments and mock user'
---

<objective>
Deepen the surveys service spec (priority deepening target) by uncommenting and fixing all 15 commented-out tests and adding new behavioral tests for scoring, participation, results, and permissions. Rewrite the filesharing service spec to test the real service class instead of a mock. Add controller tests for both modules.

Purpose: Surveys is the most complex service with scoring, participation, results, and permissions. File sharing had a critical anti-pattern (testing the mock, not the service). Both are core platform services that need thorough coverage.
Output: Deep behavioral tests for surveys and file sharing services, plus controller delegation tests for both.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-unit-test-expansion/02-RESEARCH.md
@apps/api/src/surveys/surveys.service.ts
@apps/api/src/surveys/surveys.service.spec.ts
@apps/api/src/surveys/surveys.controller.ts
@apps/api/src/surveys/surveys.controller.spec.ts
@apps/api/src/surveys/survey-answers.service.spec.ts
@apps/api/src/surveys/public-surveys.controller.spec.ts
@apps/api/src/surveys/mocks/
@apps/api/src/filesharing/filesharing.service.ts
@apps/api/src/filesharing/filesharing.service.spec.ts
@apps/api/src/filesharing/filesharing.controller.ts
@libs/src/test-utils/api-mocks/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deepen surveys service spec and related specs</name>
  <files>
    apps/api/src/surveys/surveys.service.spec.ts
    apps/api/src/surveys/surveys.controller.spec.ts
    apps/api/src/surveys/survey-answers.service.spec.ts
    apps/api/src/surveys/public-surveys.controller.spec.ts
  </files>
  <action>
**surveys.service.spec.ts -- Priority deepening target:**

1. Read the current `surveys.service.spec.ts` thoroughly. It has ~15 commented-out tests and only 2 active tests.
2. Read `surveys.service.ts` to understand all 9 public methods and their signatures.
3. Read `apps/api/src/surveys/mocks/` directory to understand existing mock data (survey mocks, user mocks, answer mocks).

Refactor the beforeEach setup to use Phase 1 infrastructure:

- Replace manual `Test.createTestingModule` with `createTestingModule` from `libs/src/test-utils/api-mocks/testingModulePreset`
- Replace manual Mongoose model mocks with `createMongooseModelMock` from `libs/src/test-utils/api-mocks/mongooseModelMock`
- Use `createJwtUser` from `libs/src/test-utils/api-mocks/jwtUserFactory` instead of local user mocks

Import paths: Check if `@libs` alias resolves in Jest. If not, use relative paths like `../../../../libs/src/test-utils/api-mocks/...`. Check existing import patterns in the codebase (e.g., how other spec files import from libs).

Uncomment all commented-out tests and fix them to work with the current API:

- Update method signatures if they changed since tests were written
- Update mock data to match current schemas
- Fix assertion expectations to match current behavior

Add NEW behavioral tests for:

- `findSurvey`: happy path (survey found), not found (returns null or throws)
- `findPublicSurvey`: happy path, survey not public (should throw or return null)
- `throwErrorIfSurveyIsNotAccessible`: accessible survey, inaccessible survey throws
- `throwErrorIfUserIsNotCreator`: creator passes, non-creator throws
- `deleteSurveys`: deletes multiple surveys, handles empty array
- `updateOrCreateSurvey`: update existing (happy path), create new (happy path), validation error path
- Scoring/participation/results: test survey answer aggregation logic if present in the service

Every public method must have at least a happy-path and one error-path test per user decision.

**surveys.controller.spec.ts deepening:**

- Read current spec and add tests for any untested controller methods
- Verify delegation: each controller method calls the correct service method with correct args
- Verify response shapes match expected DTOs

**survey-answers.service.spec.ts and public-surveys.controller.spec.ts:**

- Read current specs and add behavioral tests beyond "should be defined" if that is their sole test
- For survey-answers: test answer submission, retrieval, and scoring methods
- For public-surveys: test public endpoint access without authentication
  </action>
  <verify>
  <automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "surveys" --no-coverage -x 2>&1 | tail -30</automated>
  </verify>
  <done>surveys.service.spec.ts has all commented-out tests uncommented and passing, plus new tests for every public method with happy and error paths. Uses Phase 1 infrastructure. surveys.controller.spec.ts verifies delegation for all endpoints. survey-answers.service.spec.ts and public-surveys.controller.spec.ts have behavioral assertions.</done>
  </task>

<task type="auto">
  <name>Task 2: Rewrite filesharing service spec and create controller spec</name>
  <files>
    apps/api/src/filesharing/filesharing.service.spec.ts
    apps/api/src/filesharing/filesharing.controller.spec.ts
  </files>
  <action>
**filesharing.service.spec.ts -- Complete rewrite:**

**CRITICAL ANTI-PATTERN FIX:** The current spec provides `{ provide: FilesharingService, useValue: mockFileSharingService }` which means tests verify the mock object, not the real service. This must be completely rewritten.

1. Read `filesharing.service.ts` to understand all public methods and dependencies.
2. Read the current spec to understand what it tried to test.

Rewrite from scratch:

- Use `createTestingModule` from Phase 1 infrastructure
- Provide the REAL `FilesharingService` class (do NOT override it with a mock)
- Mock all dependencies at the service boundary:
  - `WebdavService`: mock all methods used by FilesharingService
  - `OnlyofficeService`: mock (if used)
  - `FilesystemService`: mock
  - `QueueService`: mock
  - `UsersService`: mock
  - `WebdavSharesService`: mock
  - `PublicShare` Mongoose model: use `createMongooseModelMock`
  - Any other dependencies found in the constructor

For each dependency, read which methods FilesharingService actually calls and mock only those.

Test cases (every public method gets happy-path + error-path per user decision):

- **Upload file:** Happy path (file uploaded via WebdavService), error path (WebdavService throws)
- **Delete file:** Happy path (file deleted), error path (file not found)
- **Share operations (public share CRUD):** Create public share (new PublicShare saved), read public share (found by ID), delete public share (removed)
- **Collect operations:** Test file collection flow if method exists
- **File access rights:** Test permission checking logic -- user has access vs user denied

Use `createJwtUser` for user fixtures. Use `createMongooseModelMock` for PublicShare model.

**filesharing.controller.spec.ts -- New:**

Read `filesharing.controller.ts` to understand all endpoints and their service delegations.

Create a new controller spec:

- Use `Test.createTestingModule` with `controllers: [FilesharingController]` and `providers: [{ provide: FilesharingService, useValue: mockService }]`
- Mock the service (controller tests verify delegation, not business logic)
- Test cases for each endpoint:
  - Verify correct service method is called
  - Verify correct arguments are passed (DTOs, user, file params)
  - Verify response matches service return value
  - Verify HTTP-relevant behavior (status codes if controller sets them explicitly)

Include project license header on all files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "filesharing" --no-coverage -x 2>&1 | tail -20</automated>
</verify>
<done>filesharing.service.spec.ts completely rewritten to test the REAL service with mocked dependencies. Covers upload, delete, share CRUD, collect, and access rights with happy and error paths. filesharing.controller.spec.ts created with delegation verification for all endpoints.</done>
</task>

</tasks>

<verification>
1. `npx jest --config apps/api/jest.config.ts --testPathPattern "surveys" --no-coverage` -- all survey-related specs pass
2. `npx jest --config apps/api/jest.config.ts --testPathPattern "filesharing" --no-coverage` -- all filesharing specs pass
3. No spec file has "should be defined" as its sole test
4. filesharing.service.spec.ts does NOT provide FilesharingService as a mock value (anti-pattern fixed)
5. `npm run test` -- full API test suite still passes
</verification>

<success_criteria>

- surveys.service.spec.ts has 15+ active behavioral tests (no commented-out tests remaining)
- surveys.service.spec.ts uses Phase 1 infrastructure (createTestingModule, createMongooseModelMock)
- filesharing.service.spec.ts tests the real service class with mocked dependencies
- Both controller specs verify delegation with correct arguments
- Every public method on both services has at least a happy-path and one error-path test
  </success_criteria>

<output>
After completion, create `.planning/phases/02-api-unit-test-expansion/02-03-SUMMARY.md`
</output>
