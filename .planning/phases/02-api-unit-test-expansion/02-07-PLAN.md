---
phase: 02-api-unit-test-expansion
plan: 07
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - apps/api/src/auth/auth.service.spec.ts
  - apps/api/src/auth/auth.controller.spec.ts
  - apps/api/src/webhook-clients/webhook-clients.service.spec.ts
  - apps/api/src/webhook-clients/webhook-clients.controller.spec.ts
  - apps/api/src/webhook/webhook.service.spec.ts
  - apps/api/src/webhook/webhook.controller.spec.ts
  - apps/api/src/bulletin-category/bulletin-category.controller.spec.ts
  - apps/api/src/parent-child-pairing/parent-child-pairing.controller.spec.ts
  - apps/api/src/metrics/metrics.controller.spec.ts
  - apps/api/src/groups/groups.controller.spec.ts
  - apps/api/src/sse/sse.controller.spec.ts
autonomous: true
requirements:
  - APIT-11
  - APIT-12
  - APIT-13

must_haves:
  truths:
    - 'AuthService tests cover login/validate happy path and invalid credentials'
    - 'WebhookClientsService tests cover client CRUD and secret validation'
    - 'WebhookService tests cover handleEvent with duplicate detection, eventhandler events (Redis storage), linuxmuster events, and unknown service key'
    - 'BulletinCategoryController spec verifies delegation for all 6 endpoints (findAll, create, checkName, update, remove, setPosition)'
    - 'ParentChildPairingController spec verifies delegation for all 6 endpoints (getCode, refreshCode, createParentChildPairing, getEnrichedRelationships, getAllParentChildPairings, updateParentChildPairingStatus)'
    - 'MetricsController spec verifies delegation for getMetrics endpoint'
    - 'Existing shallow spec files (groups.controller, sse.controller) are deepened with additional behavioral tests'
    - 'Every service with a controller has a corresponding controller spec verifying delegation'
    - 'No API spec file has "should be defined" as its sole test (APIT-13 verified across entire codebase)'
  artifacts:
    - apps/api/src/auth/auth.service.spec.ts
    - apps/api/src/auth/auth.controller.spec.ts
    - apps/api/src/webhook-clients/webhook-clients.service.spec.ts
    - apps/api/src/webhook-clients/webhook-clients.controller.spec.ts
    - apps/api/src/webhook/webhook.service.spec.ts
    - apps/api/src/webhook/webhook.controller.spec.ts
    - apps/api/src/bulletin-category/bulletin-category.controller.spec.ts
    - apps/api/src/parent-child-pairing/parent-child-pairing.controller.spec.ts
    - apps/api/src/metrics/metrics.controller.spec.ts
    - apps/api/src/groups/groups.controller.spec.ts
    - apps/api/src/sse/sse.controller.spec.ts
  key_links:
    - 'All service specs use createTestingModule and createMongooseModelMock from Phase 1 infrastructure'
    - 'Controller specs verify service delegation with correct arguments and user context'
    - 'webhook.controller.spec.ts tests the existing webhook.controller.ts (delegates to WebhookService and WebhookClientsService)'
    - 'webhook-clients.controller.spec.ts tests webhook-clients.controller.ts (3 endpoints: GET, POST, DELETE /webhook-clients)'
    - 'bulletin-category.controller.spec.ts tests bulletin-category.controller.ts (6 endpoints: GET, POST, GET :name, PATCH :id, DELETE :id, POST position)'
    - 'parent-child-pairing.controller.spec.ts tests parent-child-pairing.controller.ts (6 endpoints: GET code, PUT code, POST, GET relationships, GET all, PATCH :id/status)'
    - 'metrics.controller.spec.ts tests metrics.controller.ts (1 endpoint: GET /metrics with AdminGuard)'
---

<objective>
Create unit tests for Auth, WebhookClients, and Webhook services with controller specs. Add the 3 missing controller specs (bulletin-category, parent-child-pairing, metrics). Deepen existing shallow specs for groups and sse controllers. Perform final APIT-13 verification across the entire codebase.

Purpose: This plan covers authentication, webhook processing, and ensures every controller in the API has a delegation spec. The APIT-13 sweep confirms no spec file has "should be defined" as its sole test.
Output: 3 new service spec files, 6 new controller spec files, 2 deepened controller specs, and a verified clean APIT-13 result (11 files total).
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-api-unit-test-expansion/02-RESEARCH.md
@apps/api/src/auth/auth.service.ts
@apps/api/src/auth/auth.controller.ts
@apps/api/src/webhook-clients/webhook-clients.service.ts
@apps/api/src/webhook-clients/webhook-clients.controller.ts
@apps/api/src/webhook/webhook.service.ts
@apps/api/src/webhook/webhook.controller.ts
@apps/api/src/bulletin-category/bulletin-category.controller.ts
@apps/api/src/bulletin-category/bulletin-category.service.ts
@apps/api/src/parent-child-pairing/parent-child-pairing.controller.ts
@apps/api/src/parent-child-pairing/parent-child-pairing.service.ts
@apps/api/src/metrics/metrics.controller.ts
@apps/api/src/metrics/metrics.service.ts
@apps/api/src/groups/groups.controller.ts
@apps/api/src/groups/groups.controller.spec.ts
@apps/api/src/sse/sse.controller.ts
@apps/api/src/sse/sse.controller.spec.ts
@libs/src/test-utils/api-mocks/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthService, WebhookClientsService, and WebhookService specs with controllers</name>
  <files>
    apps/api/src/auth/auth.service.spec.ts
    apps/api/src/auth/auth.controller.spec.ts
    apps/api/src/webhook-clients/webhook-clients.service.spec.ts
    apps/api/src/webhook-clients/webhook-clients.controller.spec.ts
    apps/api/src/webhook/webhook.service.spec.ts
    apps/api/src/webhook/webhook.controller.spec.ts
  </files>
  <action>
**AuthService spec** (`apps/api/src/auth/auth.service.spec.ts`):
- Read source (~100 lines)
- Mock: JwtService, UsersService
- Test cases: login/validate happy path, invalid credentials

**AuthController spec** (`apps/api/src/auth/auth.controller.spec.ts`):

- Read `auth.controller.ts`
- Mock AuthService
- Verify delegation with correct arguments

**WebhookClientsService spec** (`apps/api/src/webhook-clients/webhook-clients.service.spec.ts`):

- Read source at `apps/api/src/webhook-clients/webhook-clients.service.ts` (~100 lines)
- Mock: WebhookClient Mongoose model (createMongooseModelMock)
- Test cases: create client, get clients, validate client secret, delete client

**WebhookClientsController spec** (`apps/api/src/webhook-clients/webhook-clients.controller.spec.ts`):

- Read `webhook-clients.controller.ts` (source is at `apps/api/src/webhook-clients/webhook-clients.controller.ts`)
- Controller has 3 endpoints: GET /webhook-clients (getAll), POST /webhook-clients (create with userAgent body), DELETE /webhook-clients/:id (delete)
- Mock WebhookClientsService
- Verify delegation with correct arguments for each endpoint

**WebhookService spec** (`apps/api/src/webhook/webhook.service.spec.ts`):

- Read source at `apps/api/src/webhook/webhook.service.ts` (~90 lines)
- Mock: IORedis (mock the `set`, `quit` methods). The service creates an IORedis instance in onModuleInit.
- **PITFALL**: Service uses `new IORedis(redisConnection)` in onModuleInit -- mock the IORedis constructor or the imported module
- Test cases:
  - handleEvent with eventhandler service key: stores event in Redis via `redis.set`
  - handleEvent with linuxmuster service key: logs the event (static method)
  - handleEvent with unknown service key: logs warning
  - handleEvent with duplicate eventId: throws CustomHttpException with CONFLICT status
  - markProcessed eviction: when processedEvents exceeds MAX_PROCESSED_EVENTS, oldest entry is evicted

**WebhookController spec** (`apps/api/src/webhook/webhook.controller.spec.ts`):

- Read `webhook.controller.ts` (the controller is `webhook.controller.ts`)
- Mock WebhookService (and/or WebhookClientsService if injected)
- Verify delegation with correct arguments

Include project license header on all new files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "auth.service.spec|auth.controller.spec|webhook-clients.service.spec|webhook-clients.controller.spec|webhook.service.spec|webhook.controller.spec" --no-coverage -x 2>&1 | tail -30</automated>
</verify>
<done>AuthService, WebhookClientsService, and WebhookService specs pass with behavioral assertions. WebhookController, WebhookClientsController, and AuthController specs verify delegation.</done>
</task>

<task type="auto">
  <name>Task 2: Create missing controller specs, deepen groups/sse controllers, and verify APIT-13</name>
  <files>
    apps/api/src/bulletin-category/bulletin-category.controller.spec.ts
    apps/api/src/parent-child-pairing/parent-child-pairing.controller.spec.ts
    apps/api/src/metrics/metrics.controller.spec.ts
    apps/api/src/groups/groups.controller.spec.ts
    apps/api/src/sse/sse.controller.spec.ts
  </files>
  <action>
**BulletinCategoryController spec** (`apps/api/src/bulletin-category/bulletin-category.controller.spec.ts`):
- Read `bulletin-category.controller.ts` and `bulletin-category.service.ts`
- Controller has 6 endpoints:
  - GET / (findAll) -- takes currentUser JWTUser + permission query param, delegates to `bulletinBoardService.findAll(currentUser, permission)`
  - POST / (create, AdminGuard) -- takes currentUser + body CreateBulletinCategoryDto, delegates to `bulletinBoardService.create(currentUser, bulletinCategory)`
  - GET :name (checkName, AdminGuard) -- takes name param, delegates to `bulletinBoardService.checkIfNameExists(name)`
  - PATCH :id (update, AdminGuard) -- takes id param + body, delegates to `bulletinBoardService.update(id, bulletinCategory)`
  - DELETE :id (remove, AdminGuard) -- takes id param, delegates to `bulletinBoardService.remove(id)`
  - POST position (setPosition, AdminGuard) -- takes body {categoryId, position}, delegates to `bulletinBoardService.setPosition(categoryId, position)`
- Mock BulletinCategoryService
- Verify delegation with correct arguments for each endpoint

**ParentChildPairingController spec** (`apps/api/src/parent-child-pairing/parent-child-pairing.controller.spec.ts`):

- Read `parent-child-pairing.controller.ts` and `parent-child-pairing.service.ts`
- Controller has 6 endpoints:
  - GET /code (getCode) -- takes username, groups, user (JwtUser), delegates to `parentChildPairingService.getOrCreateCode(username, groups, user.school)`
  - PUT /code (refreshCode) -- takes username, groups, user, delegates to `parentChildPairingService.refreshCode(username, groups, user.school)`
  - POST / (createParentChildPairing) -- takes username, groups, user, body (code), delegates to `parentChildPairingService.createParentChildPairing(username, groups, user.school, body.code)`
  - GET /relationships (getEnrichedRelationships) -- takes username, groups, user, delegates to `parentChildPairingService.getEnrichedRelationships(username, groups, user.school)`
  - GET /all (getAllParentChildPairings, DynamicAppAccessGuard) -- takes status? and school? query params, delegates to `parentChildPairingService.getAllParentChildPairings(status, school)`
  - PATCH :id/status (updateParentChildPairingStatus, DynamicAppAccessGuard) -- takes id param, body (status), username, lmnApiToken header, delegates to `parentChildPairingService.updateParentChildPairingStatus(id, body.status, username, lmnApiToken)`
- Mock ParentChildPairingService
- Verify delegation with correct arguments for each endpoint

**MetricsController spec** (`apps/api/src/metrics/metrics.controller.spec.ts`):

- Read `metrics.controller.ts` and `metrics.service.ts`
- Controller has 1 endpoint:
  - GET / (getMetrics, AdminGuard) -- delegates to `metricsService.getMetrics()`
- Mock MetricsService
- Verify delegation

**Deepen groups.controller.spec.ts:**

- Read current spec (3 tests) and `groups.controller.ts`
- Add tests for remaining untested controller endpoints
- Verify delegation pattern is consistent

**Deepen sse.controller.spec.ts:**

- Read current spec (3 tests) and `sse.controller.ts`
- Add tests for SSE event endpoints if applicable
- Verify delegation to SseService

**APIT-13 Final Verification:**

After all specs are written, verify that no spec file in the entire API codebase has "should be defined" as its SOLE test. Run this verification command:

```bash
for f in $(grep -rl "should be defined" apps/api/src --include='*.spec.ts'); do
  count=$(grep -c "it(" "$f");
  if [ "$count" = "1" ]; then echo "FAIL: $f has only should-be-defined"; fi;
done
```

**NOTE on existing deep specs not modified by Phase 2:** The following spec files already have deep behavioral tests beyond "should be defined" and are NOT modified by any Phase 2 plan because they are already adequate: appconfig.controller.spec.ts (7 tests), filesystem.controller.spec.ts (17 tests), users.controller.spec.ts (7 tests), vdi.controller.spec.ts (6 tests), veyon.controller.spec.ts (9 tests), lmnApi.controller.spec.ts (28 tests), tldraw-sync.controller.spec.ts (11 tests), bulletinboard.controller.spec.ts (7 tests), global-settings.controller.spec.ts (4 tests), sse.service.spec.ts (if exists), groups.service.spec.ts (14 tests). These are all already compliant with APIT-13.

Include project license header on all new files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution-ui && npx jest --config apps/api/jest.config.ts --testPathPattern "bulletin-category.controller.spec|parent-child-pairing.controller.spec|metrics.controller.spec|groups.controller.spec|sse.controller.spec" --no-coverage -x 2>&1 | tail -30</automated>
</verify>
<done>BulletinCategoryController, ParentChildPairingController, and MetricsController specs created with delegation verification. Groups and SSE controller specs deepened. APIT-13 verified: no API spec file has "should be defined" as sole test.</done>
</task>

</tasks>

<verification>
1. `npx jest --config apps/api/jest.config.ts --no-coverage` -- full API test suite passes
2. Every previously untested service module (auth, webhook-clients, webhook) now has a spec file
3. Every service spec has at least one behavioral assertion per public method
4. All 3 previously missing controller specs (bulletin-category, parent-child-pairing, metrics) now exist with delegation tests
5. No spec file has "should be defined" as its sole test: `for f in $(grep -rl "should be defined" apps/api/src --include='*.spec.ts'); do count=$(grep -c "it(" "$f"); if [ "$count" = "1" ]; then echo "FAIL: $f"; fi; done` -- produces no output
6. All controller specs verify delegation pattern
</verification>

<success_criteria>

- AuthService, WebhookClientsService, and WebhookService all have spec files with behavioral assertions
- Every service with a controller has a corresponding controller spec (Auth, WebhookClients, Webhook, BulletinCategory, ParentChildPairing, Metrics)
- BulletinCategoryController spec tests all 6 endpoints (findAll, create, checkName, update, remove, setPosition)
- ParentChildPairingController spec tests all 6 endpoints (getCode, refreshCode, createParentChildPairing, getEnrichedRelationships, getAllParentChildPairings, updateParentChildPairingStatus)
- MetricsController spec tests the getMetrics endpoint
- Existing shallow specs (groups, sse controllers) are deepened with additional tests
- No API spec file has "should be defined" as its sole test (APIT-13 satisfied across entire codebase)
- Full API test suite passes with no regressions

</success_criteria>

<output>
After completion, create `.planning/phases/02-api-unit-test-expansion/02-07-SUMMARY.md`
</output>
