---
phase: 03-frontend-unit-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/src/user/utils/getIsAdmin.spec.ts
  - libs/src/user/utils/processLdapGroups.spec.ts
  - libs/src/filesystem/utils/sanitizePath.spec.ts
  - libs/src/filesharing/utils/mapToDirectoryFiles.spec.ts
  - libs/src/filesharing/utils/formatEstimatedTimeRemaining.spec.ts
  - libs/src/filesharing/utils/convertToWebdavUrl.spec.ts
  - libs/src/filesharing/utils/buildCollectDTO.spec.ts
  - apps/frontend/src/utils/handleApiError.spec.ts
  - apps/frontend/src/utils/getDisplayName.spec.ts
  - apps/frontend/src/utils/applyThemeColors.spec.ts
  - apps/frontend/src/utils/getCompressedImage.spec.ts
  - apps/frontend/src/utils/copyToClipboard.spec.ts
  - apps/frontend/src/pages/Settings/AppConfig/schemas/getAppConfigFormSchema.spec.ts
  - apps/frontend/src/pages/Settings/AppConfig/schemas/getCustomAppConfigFormSchema.spec.ts
  - apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.spec.ts
  - apps/frontend/src/router/routes/getFramedRoutes.spec.tsx
  - apps/frontend/src/router/routes/getForwardedAppRoutes.spec.tsx
  - apps/frontend/src/router/routes/getNativeAppRoutes.spec.tsx
autonomous: true
requirements: [FEUT-01, FEUT-02, FEUT-03, FEUT-04]

must_haves:
  truths:
    - All 7 shared lib utility functions have passing tests covering normal inputs and edge cases (empty strings, null, undefined, special characters, boundary values)
    - All 5 frontend utility functions have passing tests with browser API mocks where needed
    - All 3 Zod schemas have tests confirming valid input acceptance and invalid input rejection with correct error messages
    - All 3 route generation utilities produce correct route elements for known AppConfigDto inputs
  artifacts:
    - libs/src/user/utils/getIsAdmin.spec.ts
    - libs/src/user/utils/processLdapGroups.spec.ts
    - libs/src/filesystem/utils/sanitizePath.spec.ts
    - libs/src/filesharing/utils/convertToWebdavUrl.spec.ts
    - libs/src/filesharing/utils/formatEstimatedTimeRemaining.spec.ts
    - libs/src/filesharing/utils/buildCollectDTO.spec.ts
    - libs/src/filesharing/utils/mapToDirectoryFiles.spec.ts
    - apps/frontend/src/utils/handleApiError.spec.ts
    - apps/frontend/src/utils/getDisplayName.spec.ts
    - apps/frontend/src/utils/applyThemeColors.spec.ts
    - apps/frontend/src/utils/getCompressedImage.spec.ts
    - apps/frontend/src/utils/copyToClipboard.spec.ts
    - apps/frontend/src/pages/Settings/AppConfig/schemas/getAppConfigFormSchema.spec.ts
    - apps/frontend/src/router/routes/getFramedRoutes.spec.tsx
  key_links:
    - Spec files co-located next to source files
    - All tests use vitest (describe, it, expect) with globals enabled
    - MSW server from vitest.setup.ts active for any tests that need it
---

<objective>
Test all frontend and shared-lib utility functions, Zod schemas, and route generators.

Purpose: Establish the utility test pattern for the project and verify that pure functions, schema validations, and route generators work correctly. This plan runs first (Wave 1) to create test patterns that Plans 02 and 03 follow.

Output: 18 spec files covering all FEUT requirements (FEUT-01 through FEUT-04)
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-unit-tests/03-RESEARCH.md

@libs/src/user/utils/getIsAdmin.ts
@libs/src/user/utils/processLdapGroups.ts
@libs/src/filesystem/utils/sanitizePath.ts
@libs/src/filesharing/utils/mapToDirectoryFiles.ts
@libs/src/filesharing/utils/formatEstimatedTimeRemaining.ts
@libs/src/filesharing/utils/convertToWebdavUrl.ts
@libs/src/filesharing/utils/buildCollectDTO.ts
@apps/frontend/src/utils/handleApiError.ts
@apps/frontend/src/utils/getDisplayName.ts
@apps/frontend/src/utils/applyThemeColors.ts
@apps/frontend/src/utils/getCompressedImage.ts
@apps/frontend/src/utils/copyToClipboard.ts
@apps/frontend/src/pages/Settings/AppConfig/schemas/getAppConfigFormSchema.ts
@apps/frontend/src/pages/Settings/AppConfig/schemas/getCustomAppConfigFormSchema.ts
@apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.ts
@apps/frontend/src/router/routes/getFramedRoutes.tsx
@apps/frontend/src/router/routes/getForwardedAppRoutes.tsx
@apps/frontend/src/router/routes/getNativeAppRoutes.tsx
@apps/frontend/src/**sample**/sampleMsw.spec.ts
@apps/frontend/test/vitest.setup.ts
@libs/src/test-utils/factories/createAppConfig.ts

<interfaces>
From libs/src/groups/types/group-roles.enum.ts:
const GroupRoles = { SUPER_ADMIN: 'role-globaladministrator', ... } as const;

From libs/src/lmnApi/constants/prefixes/projectsPrefix.ts:
const PROJECTS*PREFIX = '/PROJECTS/p*';

From libs/src/filesharing/utils/parseWebDAVMultiStatus.ts:
IMPORTANT: This imports Logger from @nestjs/common -- must mock @nestjs/common in test or avoid testing XML parsing directly. Test mapToDirectoryFiles by mocking parseWebDAVMultiStatus instead.

From libs/src/appconfig/constants/appIntegrationVariant.ts:
APP_INTEGRATION_VARIANT = { NATIVE: 'native', FRAME: 'frame', FORWARDING: 'forwarding' } as const;

From libs/src/appconfig/types/appConfigDto.ts:
interface AppConfigDto { name: string; appType: AppIntegrationType; icon: string; translations?: AppNameTranslations; displayLocations?: string[]; ... }

From libs/src/filesharing/utils/buildCollectPath.ts:
function buildCollectPath(cn: string, homePath: string, groupName: string, student: LmnUserInfo): CollectFileRequestDTO

From libs/src/common/constants/maximumUploadFileSize.ts:
MAXIMUM_UPLOAD_FILE_SIZE = 100 _ 1024 _ 1024 (100MB)

From libs/src/common/constants/maximumJsonBodySize.ts:
MAXIMUM_JSON_BODY_SIZE = 50 \* 1024 (50KB)

From libs/src/global-settings/types/themeColors.ts:
interface ThemeColors { primary: string; secondary: string; ciLightGreen: string; ciLightBlue: string; }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Shared lib utility tests (FEUT-01)</name>
  <files>
    libs/src/user/utils/getIsAdmin.spec.ts
    libs/src/user/utils/processLdapGroups.spec.ts
    libs/src/filesystem/utils/sanitizePath.spec.ts
    libs/src/filesharing/utils/convertToWebdavUrl.spec.ts
    libs/src/filesharing/utils/formatEstimatedTimeRemaining.spec.ts
    libs/src/filesharing/utils/buildCollectDTO.spec.ts
    libs/src/filesharing/utils/mapToDirectoryFiles.spec.ts
  </files>
  <action>
Create 7 spec files co-located next to their source files. Use vitest globals (describe, it, expect are globally available per vite.config.mts). Follow the license header pattern from existing files.

**getIsAdmin.spec.ts:** Test with table-driven it.each:

- Returns true when ldapGroups includes GroupRoles.SUPER_ADMIN
- Returns true when ldapGroups includes a group from adminGroups
- Returns false when no matching groups
- Handles empty arrays for both parameters
- Handles undefined/null adminGroups gracefully (the ?. in source)

**processLdapGroups.spec.ts:** Test regex extraction:

- Extracts school name from '/SCHOOLS/s_schoolname/' path
- Extracts class from '/SCHOOLS/s_school/students_class/className' path
- Extracts project from '/PROJECTS/p_projectname' path
- Extracts role from '/role-teacher' path
- Puts unmatched groups in 'others'
- Deduplicates schools and roles
- Returns empty arrays for empty input
- Constructs projectPaths and classPaths correctly

**sanitizePath.spec.ts:** Test path sanitization with it.each:

- Removes '..' sequences
- Collapses multiple slashes to one
- Strips leading slash
- Removes special characters (keeps a-zA-Z0-9.\_-/)
- Returns empty string for empty input
- Handles combination of all edge cases

**convertToWebdavUrl.spec.ts:** Test URL conversion:

- Converts https:// to davs://
- Converts http:// to dav://
- Returns empty string for empty input
- Leaves other protocols unchanged
- Handles URLs with paths after the protocol

**formatEstimatedTimeRemaining.spec.ts:** Mock i18n.t to return the key. Use vi.mock('i18next', ...) to provide a mock t function that returns the translation key. Test:

- Returns dash for null, undefined, NaN, negative values
- Returns fewSeconds key for < 5 seconds
- Returns lessThanMinute key for < 60 seconds
- Returns aboutOneMinute key for < 90 seconds
- Returns aboutMinutes key for < 3600 seconds
- Returns aboutHours and moreThanHours for hour ranges
- Returns aboutDays and moreThanDays for day ranges

**buildCollectDTO.spec.ts:** Test DTO construction:

- Returns undefined when students is null
- Returns undefined when currentUser is null
- Returns array of CollectFileRequestDTO when valid inputs provided (mock buildCollectPath or test integration)
- Maps each student through buildCollectPath

**mapToDirectoryFiles.spec.ts:** Mock parseWebDAVMultiStatus and parseWebDAVResponse using vi.mock since parseWebDAVMultiStatus imports @nestjs/common Logger which is not available in frontend vitest. Mock the modules at the top of the test file:

```
vi.mock('@libs/filesharing/utils/parseWebDAVMultiStatus', () => ({ default: vi.fn() }));
vi.mock('./parseWebDAVResponse', () => ({ default: vi.fn() }));
```

Then test:

- Returns parsed files when XML contains valid responses
- Filters out entries with empty filenames
- Returns empty array for empty XML (parseWebDAVMultiStatus returns [])

Run verification: `npx vitest run --config apps/frontend/vite.config.mts libs/src/user/utils/getIsAdmin.spec.ts libs/src/user/utils/processLdapGroups.spec.ts libs/src/filesystem/utils/sanitizePath.spec.ts libs/src/filesharing/utils/convertToWebdavUrl.spec.ts libs/src/filesharing/utils/formatEstimatedTimeRemaining.spec.ts libs/src/filesharing/utils/buildCollectDTO.spec.ts libs/src/filesharing/utils/mapToDirectoryFiles.spec.ts`
</action>
<verify>
<automated>npx vitest run --config apps/frontend/vite.config.mts libs/src/user/utils/getIsAdmin.spec.ts libs/src/user/utils/processLdapGroups.spec.ts libs/src/filesystem/utils/sanitizePath.spec.ts libs/src/filesharing/utils/convertToWebdavUrl.spec.ts libs/src/filesharing/utils/formatEstimatedTimeRemaining.spec.ts libs/src/filesharing/utils/buildCollectDTO.spec.ts libs/src/filesharing/utils/mapToDirectoryFiles.spec.ts</automated>
</verify>
<done>All 7 shared lib utility spec files pass with normal and edge-case coverage</done>
</task>

<task type="auto">
  <name>Task 2: Frontend utility tests (FEUT-02)</name>
  <files>
    apps/frontend/src/utils/handleApiError.spec.ts
    apps/frontend/src/utils/getDisplayName.spec.ts
    apps/frontend/src/utils/applyThemeColors.spec.ts
    apps/frontend/src/utils/getCompressedImage.spec.ts
    apps/frontend/src/utils/copyToClipboard.spec.ts
  </files>
  <action>
Create 5 spec files co-located in apps/frontend/src/utils/. Each needs module mocks for sonner and i18n at minimum.

**Common mock setup pattern for all files in this task:**

```typescript
vi.mock('sonner', () => ({ toast: { error: vi.fn(), info: vi.fn(), success: vi.fn() } }));
vi.mock('@/i18n', () => ({ default: { t: (key: string) => key }, t: (key: string) => key }));
```

**handleApiError.spec.ts:** Test error handling:

- Mock sonner.toast and i18n. Import axios for creating AxiosError instances.
- For Axios errors: calls set() with error message from response data, shows toast.error
- For PayloadTooLarge (413) with errorType 'file_upload': shows file too large message
- For PayloadTooLarge (413) without errorType: shows request too large message
- For CanceledError (error.name === 'CanceledError'): does nothing (returns early)
- For non-Axios errors: calls console.error and sets unexpectedError
- Deduplication: same error message within SHOW_TOASTER_DURATION doesn't show toast twice
- Clear the displayedErrors Set between tests by using vi.useFakeTimers and advancing past SHOW_TOASTER_DURATION, or by importing and resetting the module

**getDisplayName.spec.ts:** Test display name resolution:

- Mock i18n. Import APPS and APP_INTEGRATION_VARIANT constants.
- APPS.LINUXMUSTER with isSchoolEnvironment=true: returns 'linuxmuster.sidebarLmn' key
- APPS.LINUXMUSTER with isSchoolEnvironment=false: returns 'linuxmuster.sidebarGeneric' key
- Native app without translations: returns '{name}.sidebar' key
- App with translations: returns translation for given language
- Falls back to item.name when i18n.t returns empty

**applyThemeColors.spec.ts:** Test CSS custom property setting:

- Create a mock HTMLElement with style.setProperty as vi.fn()
- Verify setProperty called with '--primary', '--secondary', '--ci-light-green', '--ci-dark-blue'
- Verify correct theme values passed to each property

**getCompressedImage.spec.ts:** Test image compression:

- Mock FileReader, Image, Canvas, and canvas context in jsdom:
  - FileReader: mock onload trigger with a data URL result
  - Image: mock onload trigger
  - document.createElement('canvas'): return mock canvas with getContext returning mock ctx
  - ctx.drawImage: vi.fn()
  - canvas.toDataURL: return a base64 string
- Test: rejects non-image files with 'usersettings.errors.notSupportedFileFormat'
- Test: resolves with base64 string when image compresses within limit
- Test: rejects when image cannot compress below maxSizeKB
- Test: reduces dimensions for images larger than 500x500

**copyToClipboard.spec.ts:** Test clipboard operations:

- Mock navigator.clipboard.writeText
- When clipboard available and writeText succeeds: calls toast.info with success key
- When clipboard available and writeText fails: calls toast.error with error key
- When clipboard not available (navigator.clipboard undefined): calls toast.error immediately
- Supports custom toasterTranslationIds for success/error messages

Run verification: `npx vitest run --config apps/frontend/vite.config.mts apps/frontend/src/utils/`
</action>
<verify>
<automated>npx vitest run --config apps/frontend/vite.config.mts apps/frontend/src/utils/handleApiError.spec.ts apps/frontend/src/utils/getDisplayName.spec.ts apps/frontend/src/utils/applyThemeColors.spec.ts apps/frontend/src/utils/getCompressedImage.spec.ts apps/frontend/src/utils/copyToClipboard.spec.ts</automated>
</verify>
<done>All 5 frontend utility spec files pass with normal and edge-case coverage including browser API mocks</done>
</task>

<task type="auto">
  <name>Task 3: Zod schema and route generation tests (FEUT-03, FEUT-04)</name>
  <files>
    apps/frontend/src/pages/Settings/AppConfig/schemas/getAppConfigFormSchema.spec.ts
    apps/frontend/src/pages/Settings/AppConfig/schemas/getCustomAppConfigFormSchema.spec.ts
    apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.spec.ts
    apps/frontend/src/router/routes/getFramedRoutes.spec.tsx
    apps/frontend/src/router/routes/getForwardedAppRoutes.spec.tsx
    apps/frontend/src/router/routes/getNativeAppRoutes.spec.tsx
  </files>
  <action>
Create 6 spec files: 3 for Zod schemas, 3 for route generators.

**Zod Schema Tests (FEUT-03):**
Mock i18n.t as `(key: string) => key` for all schema tests since schemas use TFunction for error messages.

**getAppConfigFormSchema.spec.ts:** Create schema with mock t function, then:

- safeParse with valid record of app configs (url, proxyPath, etc.): succeeds
- safeParse with invalid URL in options.url: fails with 'common.invalid_url' message
- safeParse with forbidden proxyPath (e.g., 'auth'): fails with 'settings.errors.forbiddenProxyPath'
- safeParse with valid proxyDestination URL: succeeds
- safeParse with invalid proxyDestination: fails

**getCustomAppConfigFormSchema.spec.ts:** Create schema with mock t and mock appConfigs array:

- customAppName with valid new name: succeeds
- customAppName empty: fails with fieldRequired
- customAppName > 20 chars: fails with maxChars
- customAppName matching forbidden route (e.g., 'auth'): fails with nameIsNotAllowed
- customAppName matching existing app name (after slugify): fails with nameAlreadyExists
- customIcon empty: fails with fieldRequired

**getCreateContainerFormSchema.spec.ts:** Test dynamic schema:

- Empty envPlaceholders: returns empty object schema that accepts {}
- HOSTNAME placeholder: validates against FQDN regex (valid: 'app.example.com', invalid: 'not-a-fqdn')
- Non-HOSTNAME placeholder: validates min length 1

**Route Generation Tests (FEUT-04):**
Use .tsx extension for route spec files since they test JSX output. Import createAppConfig from factories.

**getFramedRoutes.spec.tsx:**

- Import React and Route for type checking
- Pass array with FRAME type appConfigs: returns Route elements with path matching '{name}/\*'
- Pass array with no FRAME type configs: returns empty array
- Pass mixed types: only returns FRAME routes
- Verify element key matches app name

**getForwardedAppRoutes.spec.tsx:**

- Same pattern: filters FORWARDING type, path matches '{name}' (no wildcard)
- ForwardingPage element is rendered (check element type)

**getNativeAppRoutes.spec.tsx:**

- Same pattern: filters NATIVE type, path matches '{name}/\*'
- NativeAppPageManager element with page prop set to app name

For route tests, assert on the returned React element array:

```typescript
const routes = getFramedRoutes(configs);
expect(routes).toHaveLength(1);
expect(routes[0].props.path).toBe('testapp/*');
expect(routes[0].key).toBe('testapp');
```

Run verification: `npx vitest run --config apps/frontend/vite.config.mts apps/frontend/src/pages/Settings/AppConfig/schemas/ apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.spec.ts apps/frontend/src/router/routes/`
</action>
<verify>
<automated>npx vitest run --config apps/frontend/vite.config.mts apps/frontend/src/pages/Settings/AppConfig/schemas/getAppConfigFormSchema.spec.ts apps/frontend/src/pages/Settings/AppConfig/schemas/getCustomAppConfigFormSchema.spec.ts apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.spec.ts apps/frontend/src/router/routes/getFramedRoutes.spec.tsx apps/frontend/src/router/routes/getForwardedAppRoutes.spec.tsx apps/frontend/src/router/routes/getNativeAppRoutes.spec.tsx</automated>
</verify>
<done>All 3 Zod schema specs and all 3 route generation specs pass with valid/invalid input coverage</done>
</task>

</tasks>

<verification>
- All 18 new spec files pass: `npx vitest run --config apps/frontend/vite.config.mts libs/src/user/utils/getIsAdmin.spec.ts libs/src/user/utils/processLdapGroups.spec.ts libs/src/filesystem/utils/sanitizePath.spec.ts libs/src/filesharing/utils/ apps/frontend/src/utils/ apps/frontend/src/pages/Settings/AppConfig/schemas/ apps/frontend/src/pages/Settings/AppConfig/DockerIntegration/getCreateContainerFormSchema.spec.ts apps/frontend/src/router/routes/`
- Full frontend suite still passes: `npm run test:frontend`
- No snapshot tests (behavioral assertions only)
</verification>

<success_criteria>

- 7 shared lib utility functions each have spec files with edge case coverage
- 5 frontend utility functions each have spec files with browser API mocks
- 3 Zod schemas each have spec files testing valid and invalid inputs
- 3 route generation utilities each have spec files testing filtering and output
- All tests run in < 30 seconds total
- No new dependencies added (all tools already in package.json)
  </success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-unit-tests/03-01-SUMMARY.md`
</output>
