---
phase: 03-cli-scaffolding
plan: 02
type: execute
wave: 2
depends_on: ['03-01']
files_modified:
  - edulution-ai-framework/cli/templates/custom-app/base/package.json
  - edulution-ai-framework/cli/templates/custom-app/base/vite.config.ts
  - edulution-ai-framework/cli/templates/custom-app/base/tsconfig.json
  - edulution-ai-framework/cli/templates/custom-app/base/tsconfig.app.json
  - edulution-ai-framework/cli/templates/custom-app/base/index.html
  - edulution-ai-framework/cli/templates/custom-app/base/src/main.tsx
  - edulution-ai-framework/cli/templates/custom-app/base/src/App.tsx
  - edulution-ai-framework/cli/templates/custom-app/base/src/App.css
  - edulution-ai-framework/cli/templates/custom-app/base/src/vite-env.d.ts
  - edulution-ai-framework/cli/templates/custom-app/base/public/vite.svg
  - edulution-ai-framework/cli/templates/custom-app/base/_gitignore
  - edulution-ai-framework/cli/templates/custom-app/features/tailwind/package.json
  - edulution-ai-framework/cli/templates/custom-app/features/tailwind/tailwind.config.ts
  - edulution-ai-framework/cli/templates/custom-app/features/tailwind/postcss.config.js
  - edulution-ai-framework/cli/templates/custom-app/features/tailwind/src/index.css
  - edulution-ai-framework/cli/templates/custom-app/features/api-client/package.json
  - edulution-ai-framework/cli/templates/custom-app/features/api-client/src/api/apiClient.ts
  - edulution-ai-framework/cli/templates/custom-app/features/api-client/_env.development
  - edulution-ai-framework/cli/templates/custom-app/features/auth/package.json
  - edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/AuthProvider.tsx
  - edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/useAuth.ts
  - edulution-ai-framework/cli/templates/custom-app/features/tests/package.json
  - edulution-ai-framework/cli/templates/custom-app/features/tests/vitest.config.ts
  - edulution-ai-framework/cli/templates/custom-app/features/tests/src/test/vitest.setup.ts
  - edulution-ai-framework/cli/templates/custom-app/features/tests/src/test/App.spec.tsx
autonomous: true
requirements:
  - CLI-02
  - CLI-05
  - CLI-06
  - CLI-07
  - CLI-08

must_haves:
  truths:
    - 'Base custom-app template produces a runnable Vite+React+TS project after npm install && npm run dev'
    - 'Tailwind feature overlay adds edulution design tokens (CSS custom properties for light/dark themes) and Tailwind config'
    - 'API client feature overlay provides configured axios instance with VITE_EDU_API_URL env var and withCredentials: true'
    - 'Auth feature overlay pre-wires react-oidc-context + oidc-client-ts with AuthProvider and useAuth hook'
    - 'Tests feature overlay provides Vitest + @testing-library/jest-dom setup with a sample App.spec.tsx'
    - 'ui-kit dependency works as both npm package reference and local file: reference with tsconfig paths'
    - 'Each feature overlay has its own package.json fragment that deep-merges cleanly with the base'
  artifacts:
    - path: 'edulution-ai-framework/cli/templates/custom-app/base/package.json'
      provides: 'Minimal Vite+React+TS dependencies'
      contains: 'react'
    - path: 'edulution-ai-framework/cli/templates/custom-app/base/vite.config.ts'
      provides: 'Vite config with React plugin'
      contains: '@vitejs/plugin-react'
    - path: 'edulution-ai-framework/cli/templates/custom-app/base/src/App.tsx'
      provides: 'Root React component'
      contains: 'App'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/tailwind/tailwind.config.ts'
      provides: 'Edulution Tailwind preset with CSS custom properties'
      contains: 'hsl(var(--primary))'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/tailwind/src/index.css'
      provides: 'CSS custom properties for light and dark themes'
      contains: '--ci-light-green'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/api-client/src/api/apiClient.ts'
      provides: 'Configured axios instance'
      contains: 'withCredentials'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/AuthProvider.tsx'
      provides: 'OIDC auth provider wrapping react-oidc-context'
      contains: 'AuthProvider'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/useAuth.ts'
      provides: 'Auth hook re-exporting react-oidc-context useAuth'
      contains: 'useAuth'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/tests/vitest.config.ts'
      provides: 'Vitest configuration with jsdom environment'
      contains: 'jsdom'
    - path: 'edulution-ai-framework/cli/templates/custom-app/features/tests/src/test/App.spec.tsx'
      provides: 'Sample test for App component'
      contains: 'expect'
  key_links:
    - from: 'edulution-ai-framework/cli/templates/custom-app/features/auth/package.json'
      to: 'edulution-ai-framework/cli/templates/custom-app/features/api-client/package.json'
      via: 'Auth depends on API client (axios with withCredentials needed for authenticated requests)'
      pattern: 'axios'
    - from: 'edulution-ai-framework/cli/templates/custom-app/features/tailwind/src/index.css'
      to: 'edulution-ai-framework/cli/templates/custom-app/features/tailwind/tailwind.config.ts'
      via: 'CSS variables consumed by Tailwind config color references'
      pattern: "hsl\\(var\\(--"
---

<objective>
Create all template files for the Custom React App project type: a base Vite+React+TS template and four feature overlay directories (Tailwind, API client, Auth, Tests).

Purpose: These templates are the source material the scaffold engine (from Plan 01) copies and merges to produce a working custom React app. Each feature overlay adds its dependencies via a package.json fragment and its source files in the correct directory structure.

Output: Complete `templates/custom-app/` directory with base/ and features/ subdirectories containing real, production-ready files with `{{PLACEHOLDER}}` markers where needed.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-cli-scaffolding/03-CONTEXT.md
@.planning/phases/03-cli-scaffolding/03-RESEARCH.md
@.planning/phases/03-cli-scaffolding/03-01-SUMMARY.md

Source patterns to reference for template content:

- edulution-ai-framework/custom-app/AGENTS.md (custom-app conventions: project structure, API client pattern, ui-kit usage)
- edulution-ai-framework/AGENTS.md (base conventions: design tokens table)
- edulution-ai-framework/styled-page/AGENTS.md (Tailwind config pattern with CSS custom properties)
- apps/frontend/src/index.scss (CSS custom properties -- extract light/dark theme values)
- apps/frontend/src/api/eduApi.ts (API client pattern reference)
- apps/frontend/src/App.tsx (OIDC auth pattern reference)

<interfaces>
<!-- Types created by Plan 01 that this plan's templates must align with -->

From cli/src/types.ts (Plan 01):

- PROJECT_TYPES.CUSTOM_APP = 'custom-app' (template directory name)
- Feature values: 'tailwind', 'api-client', 'auth', 'tests' (feature directory names)

From cli/src/constants.ts (Plan 01):

- RENAME_MAP: \_gitignore -> .gitignore, \_env.development -> .env.development
- PLACEHOLDER_DEFAULTS: {{PROJECT_NAME}}, {{EDU_API_URL}}, {{AUTH_CLIENT_ID}}
- TEXT_EXTENSIONS: .ts, .tsx, .js, .json, .html, .css, .md, etc.

Template structure convention:

- templates/custom-app/base/ -- always copied
- templates/custom-app/features/{feature}/ -- copied when feature selected
- package.json in each directory -- deep-merged (not file-copied)
  </interfaces>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Create base Vite+React+TS template</name>
  <files>
    edulution-ai-framework/cli/templates/custom-app/base/package.json,
    edulution-ai-framework/cli/templates/custom-app/base/vite.config.ts,
    edulution-ai-framework/cli/templates/custom-app/base/tsconfig.json,
    edulution-ai-framework/cli/templates/custom-app/base/tsconfig.app.json,
    edulution-ai-framework/cli/templates/custom-app/base/index.html,
    edulution-ai-framework/cli/templates/custom-app/base/src/main.tsx,
    edulution-ai-framework/cli/templates/custom-app/base/src/App.tsx,
    edulution-ai-framework/cli/templates/custom-app/base/src/App.css,
    edulution-ai-framework/cli/templates/custom-app/base/src/vite-env.d.ts,
    edulution-ai-framework/cli/templates/custom-app/base/public/vite.svg,
    edulution-ai-framework/cli/templates/custom-app/base/_gitignore
  </files>
  <action>
Create a minimal but runnable Vite+React+TypeScript project template at `templates/custom-app/base/`.

**package.json** -- Minimal base dependencies only (feature overlays add their own):

```json
{
  "name": "{{PROJECT_NAME}}",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0"
  }
}
```

**vite.config.ts:**

```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
});
```

**tsconfig.json:**

```json
{
  "files": [],
  "references": [{ "path": "./tsconfig.app.json" }]
}
```

**tsconfig.app.json:**

```json
{
  "compilerOptions": {
    "target": "ES2020",
    "useDefineForClassFields": true,
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "isolatedModules": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedSideEffectImports": true
  },
  "include": ["src"]
}
```

**index.html:**

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link
      rel="icon"
      type="image/svg+xml"
      href="/vite.svg"
    />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>{{PROJECT_NAME}}</title>
  </head>
  <body>
    <div id="root"></div>
    <script
      type="module"
      src="/src/main.tsx"
    ></script>
  </body>
</html>
```

**src/main.tsx:**

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

**src/App.tsx** -- Simple starter component:

```tsx
import './App.css';

const App = () => {
  return (
    <div>
      <h1>{{ PROJECT_NAME }}</h1>
      <p>Scaffolded with edulution-create</p>
    </div>
  );
};

export default App;
```

**src/App.css** -- Minimal starter styles (not Tailwind -- that comes from the feature overlay):

```css
#root {
  max-width: 1280px;
  margin: 0 auto;
  padding: 2rem;
  text-align: center;
}
```

**src/vite-env.d.ts:**

```typescript
/// <reference types="vite/client" />
```

**public/vite.svg** -- Copy the standard Vite logo SVG (the default from create-vite). This is a simple SVG file.

**\_gitignore** -- Standard Vite project gitignore (renamed to `.gitignore` during scaffold):

```
node_modules
dist
*.local
.env
.env.development
.env.production
```

  </action>
  <verify>
    <automated>test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/base/package.json && test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/base/src/main.tsx && test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/base/_gitignore && echo "Base template files exist"</automated>
  </verify>
  <done>Base custom-app template exists at templates/custom-app/base/ with package.json (React+Vite+TS deps, {{PROJECT_NAME}} placeholder), vite.config.ts, tsconfig, index.html with {{PROJECT_NAME}} in title, main.tsx, App.tsx, App.css, vite-env.d.ts, vite.svg, and _gitignore. Template is a complete minimal Vite+React+TS project that would run after placeholder replacement and npm install.</done>
</task>

<task type="auto">
  <name>Task 2: Create Tailwind and API client feature overlays</name>
  <files>
    edulution-ai-framework/cli/templates/custom-app/features/tailwind/package.json,
    edulution-ai-framework/cli/templates/custom-app/features/tailwind/tailwind.config.ts,
    edulution-ai-framework/cli/templates/custom-app/features/tailwind/postcss.config.js,
    edulution-ai-framework/cli/templates/custom-app/features/tailwind/src/index.css,
    edulution-ai-framework/cli/templates/custom-app/features/api-client/package.json,
    edulution-ai-framework/cli/templates/custom-app/features/api-client/src/api/apiClient.ts,
    edulution-ai-framework/cli/templates/custom-app/features/api-client/_env.development
  </files>
  <action>
**Tailwind feature overlay** at `templates/custom-app/features/tailwind/`:

**package.json** (fragment -- deep-merged with base):

```json
{
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "postcss": "^8.4.0",
    "autoprefixer": "^10.4.0"
  }
}
```

**tailwind.config.ts** -- The edulution Tailwind preset. Use the exact color mapping from `edulution-ai-framework/styled-page/AGENTS.md` that maps CSS custom properties to Tailwind color names. This is the key file that makes scaffolded projects look like edulution:

```typescript
import type { Config } from 'tailwindcss';

const config: Config = {
  content: ['./index.html', './src/**/*.{js,ts,jsx,tsx}'],
  darkMode: ['class'],
  theme: {
    extend: {
      colors: {
        background: 'var(--background)',
        foreground: 'var(--foreground)',
        primary: { DEFAULT: 'var(--primary)', foreground: 'var(--primary-foreground)' },
        secondary: { DEFAULT: 'var(--secondary)', foreground: 'var(--secondary-foreground)' },
        destructive: { DEFAULT: 'var(--destructive)', foreground: 'var(--destructive-foreground)' },
        muted: { DEFAULT: 'var(--muted)', foreground: 'var(--muted-foreground)' },
        accent: { DEFAULT: 'var(--accent)', foreground: 'var(--accent-foreground)' },
        popover: { DEFAULT: 'var(--popover)', foreground: 'var(--popover-foreground)' },
        card: { DEFAULT: 'var(--card)', foreground: 'var(--card-foreground)' },
        border: 'var(--border)',
        input: 'var(--input)',
        ring: 'var(--ring)',
      },
      borderRadius: {
        lg: 'var(--radius)',
        md: 'calc(var(--radius) - 2px)',
        sm: 'calc(var(--radius) - 4px)',
      },
      fontFamily: {
        sans: ['Lato', 'sans-serif'],
      },
    },
  },
  plugins: [],
};

export default config;
```

Note: Use `var(--token)` directly (not `hsl(var(--token))`) because the edulution-ui index.scss stores full color values (hex like `#0081c6`), not HSL components. The styled-page AGENTS.md shows the HSL pattern, but the actual edulution-ui source uses direct hex values in CSS custom properties. Match the actual source.

**postcss.config.js:**

```javascript
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
```

**src/index.css** -- CSS custom properties for light and dark themes. Extract the actual values from `apps/frontend/src/index.scss`. This file replaces the base App.css when Tailwind is selected (the scaffold engine copies it into src/):

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  :root {
    --radius: 0.25rem;
    --ci-light-green: #8fc046;
    --ci-dark-blue: #0081c6;
  }

  .light {
    --background: #000000;
    --foreground: #ffffff;
    --muted-foreground: #505050;
    --primary: #0081c6;
    --primary-foreground: #ffffff;
    --secondary: #2e2a24;
    --secondary-foreground: #c8c8c3;
    --muted: #b1b1ac;
    --accent: #c9ccd2;
    --input: #1d170f;
    --card: #00000000;
    --card-foreground: #000000;
    --popover: #000000;
    --popover-foreground: #f0e8d5;
    --border: #333333;
    --destructive: #ff0000;
    --destructive-foreground: #130800;
    --ring: #735b49;
    --overlay: #ffffff;
  }

  .dark {
    --background: #ffffff;
    --foreground: #000000;
    --muted-foreground: #bbbbbb;
    --primary: #0081c6;
    --primary-foreground: #ffffff;
    --secondary: #d1d5db;
    --secondary-foreground: #2d2d30;
    --muted: #2d3748;
    --accent: #2d2d30;
    --input: #e2e8f0;
    --card: #00000000;
    --card-foreground: #ffffff;
    --popover: #ffffff;
    --popover-foreground: #0f172a;
    --border: #cbd4e4;
    --destructive: #ff0000;
    --destructive-foreground: #ecf7ff;
    --ring: #8ca4b6;
    --overlay: #1e2938;
  }

  html {
    font-family: 'Lato', sans-serif;
  }
}
```

Also update the base template's `index.html` to include a Google Fonts link for Lato when Tailwind is selected -- actually, since this is a feature overlay, add a `src/index.css` import note. The main.tsx in base should import `./App.css`. When Tailwind overlay copies `src/index.css`, the executor must also ensure main.tsx imports `./index.css` instead of or in addition to `./App.css`. Handle this by having the Tailwind feature overlay include its own `src/main.tsx` that imports `./index.css` instead of `./App.css`:

Add `templates/custom-app/features/tailwind/src/main.tsx`:

```tsx
import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import './index.css';
import App from './App';

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
);
```

And add a Lato font link to a Tailwind-specific `index.html` overlay:
`templates/custom-app/features/tailwind/index.html`:

```html
<!doctype html>
<html
  lang="en"
  class="light"
>
  <head>
    <meta charset="UTF-8" />
    <link
      rel="icon"
      type="image/svg+xml"
      href="/vite.svg"
    />
    <link
      rel="preconnect"
      href="https://fonts.googleapis.com"
    />
    <link
      rel="preconnect"
      href="https://fonts.gstatic.com"
      crossorigin
    />
    <link
      href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700;900&display=swap"
      rel="stylesheet"
    />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1.0"
    />
    <title>{{PROJECT_NAME}}</title>
  </head>
  <body>
    <div id="root"></div>
    <script
      type="module"
      src="/src/main.tsx"
    ></script>
  </body>
</html>
```

This index.html adds the `class="light"` on `<html>` for theme switching and the Google Fonts link for Lato. The overlay copy overwrites the base index.html.

**API client feature overlay** at `templates/custom-app/features/api-client/`:

**package.json** (fragment):

```json
{
  "dependencies": {
    "axios": "^1.7.0"
  }
}
```

**src/api/apiClient.ts** -- Following the custom-app AGENTS.md pattern (own axios instance, not eduApi):

```typescript
import axios from 'axios';

const API_BASE_URL = import.meta.env.VITE_EDU_API_URL;

const apiClient = axios.create({
  baseURL: API_BASE_URL,
  withCredentials: true,
});

export default apiClient;
```

**\_env.development** (renamed to `.env.development` during scaffold):

```
VITE_EDU_API_URL={{EDU_API_URL}}
```

  </action>
  <verify>
    <automated>test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/features/tailwind/tailwind.config.ts && grep -q "ci-light-green" /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/features/tailwind/src/index.css && test -f /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/features/api-client/src/api/apiClient.ts && grep -q "withCredentials" /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app/features/api-client/src/api/apiClient.ts && echo "Tailwind and API client overlays OK"</automated>
  </verify>
  <done>Tailwind overlay exists with package.json fragment (tailwindcss, postcss, autoprefixer), tailwind.config.ts with edulution color mapping, postcss.config.js, index.css with light/dark theme CSS custom properties from edulution-ui source, main.tsx replacing App.css import with index.css, and index.html with Lato Google Fonts link. API client overlay exists with package.json fragment (axios), apiClient.ts with configured axios instance (withCredentials: true, VITE_EDU_API_URL), and _env.development template.</done>
</task>

<task type="auto">
  <name>Task 3: Create Auth, Tests feature overlays and verify full template set</name>
  <files>
    edulution-ai-framework/cli/templates/custom-app/features/auth/package.json,
    edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/AuthProvider.tsx,
    edulution-ai-framework/cli/templates/custom-app/features/auth/src/auth/useAuth.ts,
    edulution-ai-framework/cli/templates/custom-app/features/tests/package.json,
    edulution-ai-framework/cli/templates/custom-app/features/tests/vitest.config.ts,
    edulution-ai-framework/cli/templates/custom-app/features/tests/src/test/vitest.setup.ts,
    edulution-ai-framework/cli/templates/custom-app/features/tests/src/test/App.spec.tsx
  </files>
  <action>
**Auth feature overlay** at `templates/custom-app/features/auth/`:

**package.json** (fragment):

```json
{
  "dependencies": {
    "react-oidc-context": "^3.2.0",
    "oidc-client-ts": "^3.1.0"
  }
}
```

Note: Auth depends on API client being present (auth->api-client auto-selection in prompts). The auth overlay does NOT include axios -- that comes from the api-client overlay which is auto-selected.

**src/auth/AuthProvider.tsx** -- Based on the OIDC pattern from `apps/frontend/src/App.tsx`:

```tsx
import { AuthProvider as OidcAuthProvider } from 'react-oidc-context';
import type { AuthProviderProps } from 'react-oidc-context';
import { WebStorageStateStore } from 'oidc-client-ts';

const EDU_API_URL = import.meta.env.VITE_EDU_API_URL;

const oidcConfig: AuthProviderProps = {
  authority: `${EDU_API_URL}/auth`,
  client_id: '{{AUTH_CLIENT_ID}}',
  redirect_uri: window.location.origin,
  loadUserInfo: true,
  automaticSilentRenew: true,
  userStore: new WebStorageStateStore({ store: localStorage }),
  onSigninCallback: () => {
    window.history.replaceState({}, document.title, window.location.pathname);
  },
};

const AuthProvider = ({ children }: { children: React.ReactNode }) => (
  <OidcAuthProvider {...oidcConfig}>{children}</OidcAuthProvider>
);

export default AuthProvider;
```

**src/auth/useAuth.ts** -- Re-export the auth hook for convenient usage:

```typescript
import { useAuth } from 'react-oidc-context';

export default useAuth;
```

**Tests feature overlay** at `templates/custom-app/features/tests/`:

**package.json** (fragment):

```json
{
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0",
    "@testing-library/react": "^16.0.0",
    "@testing-library/jest-dom": "^6.6.0"
  }
}
```

**vitest.config.ts:**

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: './src/test/vitest.setup.ts',
  },
});
```

**src/test/vitest.setup.ts** -- Following the pattern from `apps/frontend/test/vitest.setup.ts`:

```typescript
import '@testing-library/jest-dom';
```

**src/test/App.spec.tsx** -- Sample test for the App component:

```tsx
import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import App from '../App';

describe('App', () => {
  it('renders the project name heading', () => {
    render(<App />);
    expect(screen.getByRole('heading', { level: 1 })).toBeInTheDocument();
  });
});
```

After creating all files, verify the complete template directory structure is present with all expected files.
</action>
<verify>
<automated>cd /home/tom/Dokumente/netzint/edulution/edulution-ui/edulution-ai-framework/cli/templates/custom-app && test -d base && test -d features/tailwind && test -d features/api-client && test -d features/auth && test -d features/tests && grep -q "react-oidc-context" features/auth/package.json && grep -q "vitest" features/tests/package.json && echo "All custom-app template directories and key files present"</automated>
</verify>
<done>Auth overlay exists with package.json fragment (react-oidc-context, oidc-client-ts), AuthProvider.tsx with OIDC config (authority from VITE_EDU_API_URL, {{AUTH_CLIENT_ID}} placeholder, WebStorageStateStore), and useAuth.ts re-export. Tests overlay exists with package.json fragment (vitest, jsdom, testing-library), vitest.config.ts with jsdom environment, vitest.setup.ts importing jest-dom matchers, and App.spec.tsx sample test. Complete custom-app template set verified: base/ + 4 feature overlays (tailwind, api-client, auth, tests).</done>
</task>

</tasks>

<verification>
- All template directories exist: `templates/custom-app/{base,features/tailwind,features/api-client,features/auth,features/tests}`
- Each feature overlay has a `package.json` fragment
- Tailwind config contains edulution CSS custom property color mapping
- index.css contains both `.light` and `.dark` theme class definitions with correct hex values from edulution-ui
- API client has `withCredentials: true` and uses `VITE_EDU_API_URL`
- Auth has `AuthProvider.tsx` with OIDC config matching edulution-ui pattern
- Tests has vitest config with jsdom and a working sample test
- No feature overlay contains dependencies that belong to another feature (separation of concerns)
</verification>

<success_criteria>

- Base template is a complete minimal Vite+React+TS project (package.json, vite.config, tsconfig, index.html, main.tsx, App.tsx)
- Each feature overlay adds only its own concerns via package.json fragment + source files
- Tailwind overlay includes full edulution theme (light/dark CSS variables, Tailwind config, Lato font)
- API client overlay provides configured axios with env var
- Auth overlay provides OIDC AuthProvider and useAuth hook
- Tests overlay provides Vitest + testing-library setup with sample test
- All {{PLACEHOLDER}} markers use the exact names from constants.ts ({{PROJECT_NAME}}, {{EDU_API_URL}}, {{AUTH_CLIENT_ID}})
  </success_criteria>

<output>
After completion, create `.planning/phases/03-cli-scaffolding/03-02-SUMMARY.md`
</output>
