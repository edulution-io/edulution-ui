---
phase: 04-frontend-components-and-e2e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/components/shared/MenuBar.spec.tsx
  - apps/frontend/src/components/ui/Sidebar/Sidebar.spec.tsx
  - apps/frontend/src/components/structure/layout/AppLayout.spec.tsx
  - apps/frontend/src/pages/LoginPage/LoginPage.spec.tsx
  - apps/frontend/src/components/ui/Table/ScrollableTable.spec.tsx
  - apps/frontend/src/pages/Surveys/Tables/CreatedSurveysPage.spec.tsx
  - apps/frontend/src/pages/Surveys/Participation/SurveyParticipationPage.spec.tsx
  - apps/frontend/src/pages/FileSharing/FileSharingPage.spec.tsx
  - apps/frontend/src/pages/Mail/MailPage.spec.tsx
autonomous: true
requirements: [FECP-01, FECP-02, FECP-03, FECP-04, FECP-05, FECP-06, FECP-07, FECP-08]

must_haves:
  truths:
    - 'MenuBar component test verifies navigation rendering, collapse behavior (isCollapsed toggling w-16 vs w-64), and active state highlighting when route matches menu item'
    - 'Sidebar component test verifies desktop vs mobile rendering based on viewport, and renders sidebar items from useSidebarItems'
    - 'AppLayout component test verifies MenuBar and Sidebar render when authenticated, hides when not authenticated, and shows OfflineBanner when isEduApiHealthy is false'
    - 'LoginPage component test verifies form validation (empty username/password shows errors), submit triggers signinResourceOwnerCredentials, and QR code toggle works'
    - 'ScrollableTable component test verifies column rendering, filter input filtering rows, sort toggling, and row selection'
    - 'Survey page tests verify loading states, data rendering from store, and navigation'
    - 'FileSharingPage test verifies breadcrumb rendering, file table display, and loading indicator'
    - 'MailPage test verifies NativeFrame iframe renders with correct appName attribute'
  artifacts:
    - path: 'apps/frontend/src/components/shared/MenuBar.spec.tsx'
      provides: 'MenuBar behavioral tests'
    - path: 'apps/frontend/src/components/ui/Sidebar/Sidebar.spec.tsx'
      provides: 'Sidebar behavioral tests'
    - path: 'apps/frontend/src/components/structure/layout/AppLayout.spec.tsx'
      provides: 'AppLayout behavioral tests'
    - path: 'apps/frontend/src/pages/LoginPage/LoginPage.spec.tsx'
      provides: 'LoginPage behavioral tests'
    - path: 'apps/frontend/src/components/ui/Table/ScrollableTable.spec.tsx'
      provides: 'Table behavioral tests'
  key_links:
    - from: 'MenuBar.spec.tsx'
      to: 'useMenuBarStore, useAppConfigsStore'
      via: 'Zustand setState + MSW handlers'
      pattern: 'useMenuBarStore.setState|server.use'
    - from: 'LoginPage.spec.tsx'
      to: 'react-oidc-context'
      via: 'vi.mock module mock'
      pattern: 'vi.mock.*react-oidc-context'
---

<objective>
Create behavioral component tests for all 8 critical UI components using React Testing Library with user-event interactions and component-specific MSW handlers.

Purpose: Verify that critical UI components render correctly, respond to user interaction, and handle state changes — catching regressions in navigation, authentication, data display, and user actions.
Output: 9 spec files covering MenuBar, Sidebar, AppLayout, LoginPage, ScrollableTable, CreatedSurveysPage, SurveyParticipationPage, FileSharingPage, and MailPage.
</objective>

<execution_context>
@/home/tom/.claude/get-shit-done/workflows/execute-plan.md
@/home/tom/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-components-and-e2e/04-RESEARCH.md

@apps/frontend/src/components/shared/MenuBar.tsx
@apps/frontend/src/components/shared/MenuBarItem.tsx
@apps/frontend/src/components/ui/Sidebar/Sidebar.tsx
@apps/frontend/src/components/structure/layout/AppLayout.tsx
@apps/frontend/src/pages/LoginPage/LoginPage.tsx
@apps/frontend/src/components/ui/Table/ScrollableTable.tsx
@apps/frontend/src/components/ui/Table/TableGridView.tsx
@apps/frontend/src/pages/Surveys/Tables/CreatedSurveysPage.tsx
@apps/frontend/src/pages/Surveys/Participation/SurveyParticipationPage.tsx
@apps/frontend/src/pages/FileSharing/FileSharingPage.tsx
@apps/frontend/src/pages/Mail/MailPage.tsx
@apps/frontend/test/vitest.setup.ts
@libs/src/test-utils/providers/renderWithProviders.tsx
@libs/src/test-utils/msw/server.ts
@libs/src/test-utils/msw/handlers/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Navigation and layout component tests (MenuBar, Sidebar, AppLayout)</name>
  <files>
    apps/frontend/src/components/shared/MenuBar.spec.tsx
    apps/frontend/src/components/ui/Sidebar/Sidebar.spec.tsx
    apps/frontend/src/components/structure/layout/AppLayout.spec.tsx
  </files>
  <action>
Create 3 co-located component spec files using renderWithProviders, user-event, and component-specific MSW handlers.

**MenuBar.spec.tsx** — Test the MenuBar component:

1. Mock `useMenuBarConfig` to return a controlled MenuBarEntry with menuItems, title, icon, color, disabled:false, appName
2. Use component-specific MSW handlers for `/edu-api/appconfig` and `/edu-api/auth/userinfo`
3. Set Zustand store state directly via `useMenuBarStore.setState()` before each test
4. Mock `useMedia` hook to control isMobileView/isTabletView
5. Tests:
   - Renders menu items from config (verify items visible by role/text)
   - Collapse behavior: when `isCollapsed: true`, menu shrinks (verify collapsed class `w-16` present, labels not rendered)
   - Expand behavior: when `isCollapsed: false`, labels render alongside icons
   - Active state: when route matches menu item, active color class is applied
   - Mobile: when isMobileView, renders mobile overlay (translate-x classes)
   - Disabled: when menuBarEntries.disabled is true, returns null (nothing renders)
   - Click outside closes mobile menu (trigger click outside menubarRef)

**Sidebar.spec.tsx** — Test the Sidebar component:

1. Mock `useSidebarItems` to return controlled sidebar item list
2. Mock `useMedia` to control isMobileView/isTabletView
3. Mock `usePlatformStore` for isEdulutionApp
4. Tests:
   - Desktop view (isMobileView=false, isTabletView=false): renders DesktopSidebar
   - Mobile view (isMobileView=true): renders MobileSidebar
   - Tablet view (isTabletView=true): renders MobileSidebar
   - Always renders NotificationPanel
   - Passes sidebarItems to both desktop and mobile variants

**AppLayout.spec.tsx** — Test the AppLayout component:

1. Mock `useUserStore` for isAuthenticated state
2. Mock `useAppConfigsStore` with appConfigs (set to something other than APPS.NONE for ready state)
3. Mock `useEduApiStore` for isEduApiHealthy
4. Mock `useMenuBarConfig` to return non-disabled config
5. Use MemoryRouter with Outlet support (wrap in Routes/Route)
6. Tests:
   - Authenticated user sees MenuBar and Sidebar
   - Unauthenticated user: Sidebar hidden (isAuthenticatedAppReady is false)
   - When isEduApiHealthy is false, OfflineBanner renders
   - When menuBar.disabled is true, MenuBar not rendered
   - Outlet renders child route content

IMPORTANT: All three specs must use full tree rendering (no shallow/mocking child components per locked decision). Each spec defines its own MSW handlers via `server.use()` in beforeEach — do NOT import shared handlers from libs/src/test-utils/msw/handlers. Use `vi.mock()` for hooks that cannot be driven via MSW (useMedia, useMenuBarConfig, useSidebarItems).
</action>
<verify>
<automated>npx vitest run apps/frontend/src/components/shared/MenuBar.spec.tsx apps/frontend/src/components/ui/Sidebar/Sidebar.spec.tsx apps/frontend/src/components/structure/layout/AppLayout.spec.tsx --reporter=verbose</automated>
</verify>
<done>
MenuBar spec has 7+ tests covering rendering, collapse, expand, active state, mobile, disabled, and click-outside.
Sidebar spec has 5+ tests covering desktop, mobile, tablet, NotificationPanel, and item passing.
AppLayout spec has 5+ tests covering authenticated, unauthenticated, offline banner, disabled menubar, and outlet rendering.
All tests pass with `npm run test:frontend`.
</done>
</task>

<task type="auto">
  <name>Task 2: LoginPage and Table component tests</name>
  <files>
    apps/frontend/src/pages/LoginPage/LoginPage.spec.tsx
    apps/frontend/src/components/ui/Table/ScrollableTable.spec.tsx
  </files>
  <action>
Create 2 co-located component spec files.

**LoginPage.spec.tsx** — Test the LoginPage component:

1. Mock `react-oidc-context` at module level with `vi.mock('react-oidc-context')`:
   - Return a mock `useAuth()` with controllable `isAuthenticated`, `isLoading`, `user`, `signinResourceOwnerCredentials`, `error`
   - Default: isAuthenticated=false, isLoading=false, user=null
2. Mock multiple stores: `useUserStore` (eduApiToken, isAuthenticated, createOrUpdateUser, setEduApiToken, getTotpStatus), `useLmnApiStore`, `useSessionFlagsStore`, `useGlobalSettingsApiStore`, `useAppConfigsStore`
3. Mock hooks: `useDeploymentTarget` (return isLmn:false, isGeneric:true), `useSilentLoginWithPassword`, `useAuthErrorHandler`
4. Component-specific MSW handlers for any API calls
5. Tests:
   - Renders login form with username and password fields (find by data-testid: `test-id-login-page-username-input`, `test-id-login-page-password-input`)
   - Renders submit button (data-testid: `test-id-login-page-submit-button`)
   - Form validation: submit with empty fields triggers validation errors (username and password required)
   - Submit with valid credentials calls `signinResourceOwnerCredentials` with username and base64-encoded password
   - When isLoading is true, submit button shows loading text and is disabled
   - QR code toggle: clicking the app login button shows QR code display and hides form fields
   - When auth.isAuthenticated becomes true, navigates away from login page
   - TOTP flow: when getTotpStatus returns true, shows TOTP input

**ScrollableTable.spec.tsx** — Test the ScrollableTable component:

1. Create mock column definitions using `ColumnDef` from @tanstack/react-table (at least 3 columns: name, email, status)
2. Create mock data array (5+ items) with typed objects
3. No MSW needed — Table is props-driven
4. Tests:
   - Renders column headers from column definitions
   - Renders all data rows
   - Filter input: typing in filter field reduces visible rows (use filterKey matching a column)
   - Row selection: clicking row checkbox changes selection state (verify onRowSelectionChange callback)
   - Search bar visibility: when showSearchBarAndColumnSelect=false, search input not rendered
   - Loading state: when isLoading=true and data is empty, shows LoadingIndicatorDialog
   - Actions: when actions prop provided, TableActionFooter renders action buttons
     </action>
     <verify>
     <automated>npx vitest run apps/frontend/src/pages/LoginPage/LoginPage.spec.tsx apps/frontend/src/components/ui/Table/ScrollableTable.spec.tsx --reporter=verbose</automated>
     </verify>
     <done>
     LoginPage spec has 8+ tests covering form rendering, validation, submit, loading, QR toggle, auth redirect, and TOTP.
     ScrollableTable spec has 7+ tests covering headers, rows, filtering, selection, search bar, loading, and actions.
     All tests pass with `npm run test:frontend`.
     </done>
     </task>

<task type="auto">
  <name>Task 3: Survey, FileSharing, and Mail component tests</name>
  <files>
    apps/frontend/src/pages/Surveys/Tables/CreatedSurveysPage.spec.tsx
    apps/frontend/src/pages/Surveys/Participation/SurveyParticipationPage.spec.tsx
    apps/frontend/src/pages/FileSharing/FileSharingPage.spec.tsx
    apps/frontend/src/pages/Mail/MailPage.spec.tsx
  </files>
  <action>
Create 4 co-located component spec files.

**CreatedSurveysPage.spec.tsx** — Test the CreatedSurveysPage component:

1. Mock `useSurveyTablesPageStore` to return controlled state: createdSurveys, isFetchingCreatedSurveys, updateCreatedSurveys, selectSurvey, setSelectedRows, hasAnswers, canParticipate
2. Use component-specific MSW handlers for survey API endpoints
3. Tests:
   - Shows loading dialog when isFetchingCreatedSurveys is true
   - Calls updateCreatedSurveys on mount
   - Renders SurveyTablePage with correct props (title, description, canDelete:true, canEdit:true)
   - When surveys are loaded, passes them to SurveyTablePage
   - Resets selectedRows and selectSurvey on mount

**SurveyParticipationPage.spec.tsx** — Test the SurveyParticipationPage component:

1. Mock `useSurveyTablesPageStore` for fetchSelectedSurvey, selectedSurvey, isFetching, reset
2. Mock `useParticipateSurveyStore` for reset
3. Mock `react-router-dom` useParams to return surveyId
4. Tests:
   - When surveyId is undefined, shows "survey.notFound" message
   - When isFetching is true, shows CircleLoader
   - When selectedSurvey is null after fetch, shows "survey.notFound"
   - When selectedSurvey exists, renders AccessAndParticipateSurvey component
   - Calls fetchSelectedSurvey with surveyId on mount
   - Calls reset functions on mount

**FileSharingPage.spec.tsx** — Test the FileSharingPage component:

1. Mock `useFileSharingPage` hook to return controlled state: isFileProcessing, currentPath, searchParams, setSearchParams, isLoading
2. Mock `useFileSharingStore`, `useFileEditorStore`, `usePublicShareStore`, and other FileSharing hooks
3. Mock `useMedia` for isMobileView
4. Mock `useQuotaInfo` for percentageUsed
5. Tests:
   - Shows LoadingIndicatorDialog when isLoading is true
   - Renders DirectoryBreadcrumb with currentPath
   - Renders FileSharingTable
   - Renders QuotaLimitInfo with percentage
   - When isFileProcessing is true, shows HorizontalLoader
   - When isFilePreviewVisible is true, renders file preview container

**MailPage.spec.tsx** — Test the MailPage component:

1. MailPage renders `<NativeFrame appName={APPS.MAIL} />` — it is an iframe wrapper
2. Mock the NativeFrame component or verify its rendering
3. Tests:
   - Renders NativeFrame with appName equal to APPS.MAIL constant
   - Verify the component renders without errors

IMPORTANT for Survey tests: Mock `survey-creator-react` and `survey-creator-core` at module level if any survey component imports them. Do NOT attempt to test SurveyJS internal behavior — test page-level logic only (loading states, navigation, dialog triggers).
</action>
<verify>
<automated>npx vitest run apps/frontend/src/pages/Surveys/Tables/CreatedSurveysPage.spec.tsx apps/frontend/src/pages/Surveys/Participation/SurveyParticipationPage.spec.tsx apps/frontend/src/pages/FileSharing/FileSharingPage.spec.tsx apps/frontend/src/pages/Mail/MailPage.spec.tsx --reporter=verbose</automated>
</verify>
<done>
CreatedSurveysPage spec has 5+ tests covering loading, mount calls, props passing, and data rendering.
SurveyParticipationPage spec has 6+ tests covering no-surveyId, loading, not-found, found, fetch on mount, and reset.
FileSharingPage spec has 6+ tests covering loading, breadcrumb, table, quota, file processing, and file preview.
MailPage spec has 2+ tests covering NativeFrame rendering and appName prop.
All tests pass with `npm run test:frontend`.
</done>
</task>

</tasks>

<verification>
After all 3 tasks complete:
1. Run full frontend test suite: `npm run test:frontend` — all tests pass including new component specs
2. Verify no regressions in existing Phase 3 tests
3. Count: At least 45 new tests across 9 spec files
4. All tests use user-event (not fireEvent), renderWithProviders, and component-specific MSW handlers
5. No snapshot assertions used
</verification>

<success_criteria>

- 9 new spec files created, co-located with their source components
- MenuBar tests cover: rendering, collapse, expand, active state, mobile, disabled (FECP-01)
- Sidebar tests cover: desktop/mobile switching, item rendering (FECP-02)
- AppLayout tests cover: authenticated layout, unauthenticated layout, offline banner (FECP-03)
- LoginPage tests cover: form validation, OIDC submit, loading state, QR code, TOTP (FECP-04)
- ScrollableTable tests cover: column rendering, filtering, sorting, selection (FECP-05)
- Survey tests cover: loading states, data display, participation flow (FECP-06)
- FileSharingPage tests cover: file listing, breadcrumb, loading, file preview (FECP-07)
- MailPage tests cover: iframe rendering with correct appName (FECP-08)
- `npm run test:frontend` passes with 0 failures
  </success_criteria>

<output>
After completion, create `.planning/phases/04-frontend-components-and-e2e/04-01-SUMMARY.md`
</output>
