const logoutScript = `
    const logoutButton = document.querySelector('button.btn .fas.fa-sign-out-alt');

    if (logoutButton) {
      logoutButton.click();
    }
  `;

export default logoutScript;
