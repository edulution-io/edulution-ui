export enum UserManagementEnum {
  WIFI = 'wifi',
  Internet = 'internet',
  PRINTER = 'printer',
  EXAM = 'Exam',
}

export enum ItemTypes {
  PROJECT = 'project',
  SESSION = 'session',
  CLASS = 'class',
}
