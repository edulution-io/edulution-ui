version: '3.8'

services:
  redis-test:
    image: redis:7-alpine
    ports:
      - '6380:6379'
    command: redis-server --appendonly no --save ""
    healthcheck:
      test: ['CMD', 'redis-cli', 'ping']
      interval: 5s
      timeout: 3s
      retries: 5
    tmpfs:
      - /data
